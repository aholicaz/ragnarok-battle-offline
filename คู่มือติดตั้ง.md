# Ragnarok Battle Offline — คู่มือระบบเกม

ระบบทั้งหมดถูกติดตั้งและ **เชื่อมเข้ากับฉาก/สไปรท์ที่คุณทำไว้แล้ว**

> ผ่านการทดสอบรันจริงบน Godot headless แล้ว 27 เคส — ไม่มี error

---

## ⚠ อ่านก่อนเป็นอันดับแรก

**ปิด Godot แล้วเปิดใหม่** ก่อนกดเล่น

เพราะ Godot อ่าน `project.godot` ตอนเปิดโปรแกรมครั้งเดียว ตัว Autoload ทั้ง 6 ตัวที่เพิ่งเพิ่มเข้าไปจะยังไม่ทำงานถ้าไม่รีสตาร์ท
และ **ห้ามกด Ctrl+S ทับฉาก `world_node_2d` ก่อนปิด** เพราะจะเขียนทับของเดิมที่ผมแก้ไว้

---

## 1. เล่นเลย

เปิด Godot ใหม่ → กด **F5** → เริ่มที่ฉากเดิมของคุณ (พื้นหลังป่า + พรอนเทรา)

| ปุ่ม | ทำอะไร |
|---|---|
| ← → | เดิน |
| Space | กระโดด |
| **J หรือ X** | โจมตี (J เหมือนเดิมที่คุณตั้งไว้) |
| 1 2 3 4 | ใช้สกิลที่ตั้งไว้ |
| Z | เก็บไอเทม |
| Q | กินยาแดงด่วน |
| **F** | คุย NPC / เข้าประตู |
| **C** | หน้าต่างสเตตัส (อัพ STR/AGI/VIT/INT/DEX/LUK) |
| **I** | กระเป๋า |
| **E** | สวมใส่อุปกรณ์ |
| **K** | สกิล (เรียน + ตั้งปุ่มลัด) |
| **V** | อัลบั้มการ์ดมอนสเตอร์ |
| Esc | ปิดหน้าต่างทั้งหมด |
| F5 / F9 | เซฟ / โหลด |

### ลองตามนี้
1. ตีโพริง 4 ตัวที่เกิดกลางแมพ → ได้ EXP, ซีนี, ไอเทมดรอป (เดินทับเก็บอัตโนมัติ)
2. กด **C** → อัพ STR ดู ATK เพิ่มทันที (มี 20 แต้มให้ตั้งแต่เริ่ม)
3. กด **K** → เรียน Bash → กดเลข 1 ตั้งเป็นปุ่มลัด → กลับไปกด 1 ใส่โพริง
4. เดินไปขวาสุดของแมพ → กดผ่านประตู **"→ เมือง"** → เจอพ่อค้า/ช่างตีเหล็ก/หมอ/จุดเซฟ
5. คุยช่างตีเหล็ก (F) → ตีบวกดาบมือใหม่

---

## 2. ผมทำอะไรกับฉากของคุณบ้าง

`Sprites/world_node_2d.tscn` ยังเป็นฉากเดิม แต่เปลี่ยนให้เป็น "แมพ" ของระบบใหม่

| ของเดิม | ตอนนี้ |
|---|---|
| TileMap + พื้นหลัง + StaticBody2D | **เก็บไว้เหมือนเดิมทุกอย่าง** |
| Node Player ที่ฝังในฉาก | ถอดออก → ระบบเกิดผู้เล่นให้เอง ใช้ `scenes/player/player.tscn` |
| Poring ที่วางไว้ 1 ตัว (ติดกลุ่ม player ผิด) | ถอดออก → ใช้ `Spawners/PoringSpawner` เกิด 4 ตัวและเกิดใหม่เอง |
| — | เพิ่ม `SpawnPoints` (จุดเกิดผู้เล่น) และ `Portals/ToTown` |
| สคริปต์ `world_node_2d.gd` | เปลี่ยนเป็น `scripts/world/map_base.gd` (กล้องตาม + จำกัดขอบแมพ) |

**สไปรท์ของคุณถูกดึงออกมาเป็นไฟล์ใช้ซ้ำได้:**
- `data/sprites/player_frames.tres` — Idle (s1-s6), Run (r1-r6), Attack (f1-f6)
- `data/sprites/poring_frames.tres` — Idle, Attack, Jump

ไฟล์เดิมที่ไม่ได้ใช้แล้วแต่ยังเก็บไว้: `Sprites/world_node_2d.gd`, `Sprites/player.gd`, `poring.gd`, `attack.gd`, `Sprites/poring.tscn`, `Sprites/attack.tscn` (ลบทิ้งได้ถ้าไม่ใช้)

> ถ้าอนิเมชันไหนยังไม่มี (เช่น Death, Hit ของผู้เล่น) ระบบจะใช้ตัวสำรองแทนอัตโนมัติ ไม่ค้างไม่หาย
> ทำเพิ่มเมื่อไหร่ก็แค่เพิ่มอนิเมชันชื่อ `Hit` / `Death` / `Jump` ลงใน `data/sprites/player_frames.tres` แล้วมันจะใช้เองทันที

---

## 3. แมพที่มีตอนนี้

| id | ไฟล์ | สภาพ |
|---|---|---|
| `prontera_field` | **`Sprites/world_node_2d.tscn` (ฉากของคุณ)** | มีพื้นหลังสวยแล้ว มีโพริง 4 ตัว + ประตูไปเมือง |
| `prontera_town` | `scenes/maps/prontera_town.tscn` | พื้นเป็นสี่เหลี่ยมสีเปล่า ๆ มี NPC ครบ 5 ตัว รอคุณเอาภาพเมืองมาทับ |
| `dark_forest` | `scenes/maps/dark_forest.tscn` | พื้นเปล่า มีมอน 7 ชนิดรวมบอสบาฟโฟเมท ยังไม่มีประตูเชื่อม |

ทะเบียนแมพอยู่ที่ `scripts/core/game.gd` (const `MAPS`) — เพิ่มแมพใหม่ = เพิ่ม 1 บรรทัด
`scenes/maps/prontera_field.tscn` เป็นแมพเปล่าที่ผมทำไว้เป็นตัวอย่าง ตอนนี้ไม่ได้ใช้แล้ว เก็บไว้ดูโครงสร้างได้

---

## 3.5 ★ วิธีทำให้แมพกว้างขึ้น

ตอนนี้แมพของคุณเปิด **Auto Fit Bounds** ไว้แล้ว — ระบบจะวัดขนาดแมพจากกระเบื้องที่วาด
กับกล่องชนของพื้นให้เอง **ไม่ต้องมานั่งแก้ Map Bounds ทุกครั้ง**

> ขนาดที่วัดได้ตอนนี้: กว้าง **1664** สูง **988** (x จาก -360 ถึง 1304)
> พื้นที่เดินได้จริงแค่ x -153 ถึง 1133 เพราะกล่องชนของพื้นสั้นกว่าภาพพื้นหลัง

### ทำให้กว้างขึ้นแค่ 2 ขั้น

**ขั้นที่ 1 — ยืดพื้นให้ยาวขึ้น**
เปิด `Sprites/world_node_2d.tscn` → เลือก `TileMap/StaticBody2D/CollisionShape2D`
ใน Inspector: `Shape > Size` ตอนนี้เป็น `(1286, 30)`
- อยากได้ยาว 2 เท่า → เปลี่ยนเป็น `(2572, 30)`
- แล้วเลื่อน `Position.x` ไปครึ่งหนึ่งของส่วนที่เพิ่ม (จาก `490` เป็น `490 + 643 = 1133`)

หรือถ้าอยากทำเป็นหลายแท่น: คลิกขวา `TileMap` → Add Child Node → `StaticBody2D`
→ ใส่ `CollisionShape2D` + `RectangleShape2D` ข้างใน แล้ววางตรงไหนก็ได้
ตั้ง `Collision Layer = 1`, `Collision Mask = 0`

**ขั้นที่ 2 — วาดพื้นหลังต่อ**
เลือก `TileMap` → เปิดแท็บ **TileMap** ด้านล่างจอ → เลือกกระเบื้องแล้ววาดต่อไปทางขวา
(ภาพพื้นหลังของคุณถูกตัดเป็นกระเบื้อง 16x16 อยู่แล้ว วาดซ้ำเป็นฉากต่อเนื่องได้เลย)

จบ — กด F5 กล้องจะตามไปได้ไกลขึ้นเองทันที ไม่ต้องแก้โค้ดหรือ Map Bounds

### ถ้าอยากกำหนดขอบเองแทน
ที่ node `WorldNode2D` ปิด `Auto Fit Bounds` แล้วใส่ `Map Bounds` เอง เช่น
`Rect2(-360, -324, 4000, 988)` = แมพกว้าง 4000 พิกเซล

### เอามอนไปเกิดในพื้นที่ใหม่
คลิกขวา `Spawners` → Add Child Node → `Node2D` → ลากสคริปต์ `scripts/world/monster_spawner.gd` ใส่
→ วางตำแหน่ง → ใส่ `Monster Types` + `Monster Scene` (`scenes/monsters/monster.tscn`)
→ ตั้ง `Max Alive` กับ `Spawn Width`

---

## 4. โครงสร้างโปรเจกต์

```
data/                  ← ★ ปรับค่าเกมทั้งหมดที่นี่ แก้ใน Inspector ได้เลย
  monsters/            มอนสเตอร์ 11 ตัว (poring ผูกสไปรท์ของคุณแล้ว)
  items/               ไอเทม 33 ชิ้น
  skills/              สกิล 7 ตัว
  jobs/                อาชีพ 2 อาชีพ
  sprites/             SpriteFrames ที่ดึงจากฉากของคุณ
scripts/
  resources/  แม่แบบข้อมูล   core/  หัวใจเกม
  entities/   ผู้เล่น+มอน     ui/    หน้าต่างทุกบาน (สร้างด้วยโค้ด)
  world/      แมพ/spawner/ประตู/NPC
scenes/
  player/player.tscn      ← ใช้สไปรท์ของคุณแล้ว
  monsters/monster.tscn   ← มอนทุกตัวใช้ Scene นี้ตัวเดียว
  items/ npc/ world/ maps/
```

Autoload 6 ตัว: `Events` → `GameData` → `Game` → `PlayerState` → `SaveManager` → `UI`

---

## 5. ★ ปรับค่าพลังมอนสเตอร์

เปิด `.tres` ใน `data/monsters/` แก้ใน Inspector ได้เลย ไม่ต้องแตะโค้ด

| กลุ่ม | ปรับอะไรได้ |
|---|---|
| Visual | SpriteFrames, ขนาด, offset, กล่องชน, ตำแหน่งหลอดเลือด |
| Core Stats | เลเวล, HP, ATK ต่ำ-สูง, DEF, MDEF, HIT, FLEE, CRIT |
| Type | ธาตุ, เผ่าพันธุ์, ขนาด (มีผลกับตารางธาตุ 10x10) |
| Movement & AI | ความเร็ว, กระโดดไหม, ระยะเห็นผู้เล่น, ระยะโจมตี, ระยะห่างจุดเกิดสูงสุด |
| **การเดินเล่น** | `wander_speed` เดินเร็วแค่ไหน · `wander_range` เดินห่างบ้านได้กี่พิกเซล · `hop_while_wandering` เด้งตอนเดินไหม · `jump_interval` เว้นกี่วินาทีถึงเด้งอีกที |
| Attack Timing | จังหวะดาเมจออก, ความยาวท่า, คูลดาวน์, แรงกระเด็น |
| Reward | EXP, ซีนี, ตารางดรอป |
| Respawn | เกิดใหม่กี่วินาที |

**HIT / FLEE เป็น "โบนัส"** ระบบบวก `100 + เลเวล` ให้เอง เพื่อเทียบกับผู้เล่น (`100 + Lv + Dex/Agi`)
เกณฑ์: `hit ≈ เลเวล × 1.5`, `flee ≈ เลเวล × 1.2`

### เพิ่มมอนใหม่ (ไม่ต้องเขียนโค้ด)
1. คลิกขวาใน `data/monsters/` → New Resource → **MonsterData**
2. ตั้ง `id` (ห้ามซ้ำ), ชื่อ, SpriteFrames, ค่าพลัง, ดรอป
3. เปิดแมพ → เลือก node ใน `Spawners` → ลากไฟล์ใหม่ใส่ช่อง **Monster Types**

### ตั้งค่าดรอป
ช่อง **Drops** กด `+` → New **DropEntry** → `item_id`, `chance` (%), `min/max_count`, `min/max_refine`

---

## 5.5 พฤติกรรมมอนสเตอร์

- **เดินเล่นอิสระ**: มอนจะสุ่มเดินซ้าย-ขวา-ยืนพัก รอบ ๆ จุดเกิดของตัวเอง
  ระยะเดินกำหนดที่ `wander_range` ของมอนแต่ละตัว
- **ไม่เดินตกเหว**: ระบบยิงเรย์เช็คพื้นข้างหน้า ถ้าเป็นขอบจะกลับตัวเอง
- **เด้งตามจังหวะ**: โพริงจะเด้งเป็นจังหวะ ไม่เด้งรัวทุกเฟรม (คุมด้วย `jump_interval`)
- **มอนใจดี (PASSIVE)** จะไม่ไล่ผู้เล่นจนกว่าจะถูกตี พอถูกตีจะจำไว้ 8 วินาที
  แล้วค่อยกลับไปเดินเล่นตามปกติ
- **มอนดุ (AGGRESSIVE)** เห็นในระยะ `detect_range` แล้วไล่ทันที
- ทั้งหมดจะไม่ไล่เลย `leash_range` จากจุดเกิด แล้วจะเดินกลับบ้านเอง

### ป็อปอัพยืนยันเข้าประตู
เดินชนประตูแล้วจะมีกล่องถามก่อน (เกมหยุดรอ) — กด **Enter/F = ไปเลย**, **Esc = ยังก่อน**
กดยกเลิกแล้วยืนตรงประตูกด **F** ถามใหม่ได้ หรือเดินออกแล้วเดินเข้าใหม่

ปิดป็อปอัพได้ที่ node `Portal` → ติ๊ก `Require Confirm` ออก
(แล้วเลือกต่อว่า `Auto Enter` เปิด = ชนแล้วไปเลย / ปิด = ต้องกด F)

---

## 6. ระบบสเตตัส (สูตร RO)

| สเตตัส | ให้อะไร |
|---|---|
| **STR** | ATK (โตแบบ `STR + (STR/10)²` ทุก 10 แต้มคุ้มขึ้น) |
| **AGI** | FLEE + ความเร็วโจมตี (ASPD) |
| **VIT** | MaxHP + DEF + ฟื้นเลือด |
| **INT** | MaxSP + MATK + ฟื้น SP + พลังสกิลฟื้นเลือด |
| **DEX** | HIT + ATK เล็กน้อย + ASPD เล็กน้อย |
| **LUK** | CRIT + ATK เล็กน้อย |

- ค่าอัพ = `⌊(ค่าปัจจุบัน−1)/10⌋ + 2`
- เลเวลขึ้น 1 ครั้ง ได้ `3 + ⌊Lv/5⌋` Stat Point + 1 Skill Point
- ตัวคูณของอาชีพแก้ที่ `data/jobs/swordsman.tres`

---

## 7. สกิล / ไอเทม / ตีบวก

**สกิล 7 ตัว**: Sword Mastery, HP Recovery, Bash, Magnum Break, Endure, Battle Cry, First Aid
มีเงื่อนไขสกิลบังคับก่อน (Magnum Break ต้องมี Bash Lv5 + ตัวละคร Lv10)

**กระเป๋า 40 ช่อง** / **สวมใส่ 8 ช่อง** (อาวุธ, มือรอง, หัว, เกราะ, ผ้าคลุม, รองเท้า, เครื่องประดับ×2)

**ตีบวก +0→+10**: อัตราสำเร็จ 100/100/100/90/80/65/50/35/25/15 %
ใช้ฟราคอน (+0→+4) และเอ็มเวเรทาร์คอน (+5 ขึ้นไป) — ตั้งแต่ +7 พลาดแล้วลด 1 ขั้น
ปรับความยากที่ `const` หัวไฟล์ `scripts/core/refine_system.gd`

---

## 7.5 ★ ระบบการ์ดมอนสเตอร์

มอนสเตอร์ **ทุกตัว** มีการ์ดของตัวเอง ดรอปที่ **5%** (ทดสอบสุ่ม 4000 ครั้งได้ 5.05% ตรงตามที่ตั้ง)

### กด V เปิดอัลบั้มการ์ด
- โชว์การ์ดทุกใบในเกม ใบที่ยังไม่ได้จะเป็น **เงาดำ + ชื่อ ???**
- ใบที่เก็บได้แล้วจะโชว์ **รูปมอนสเตอร์ตัวนั้นในกรอบการ์ด** แบบ Ragnarok
  พร้อมชื่อ คุณสมบัติ ช่องที่ใส่ได้ และระดับความหายาก
- นับความคืบหน้าให้ด้วย เช่น "เก็บได้ 3 / 11 ใบ"

### ใส่การ์ดลงอุปกรณ์
1. กด **V** → เลือกการ์ดที่มี
2. ใต้ภาพการ์ดจะมีปุ่ม **"ใส่ลงใน:"** พร้อมรายชื่ออุปกรณ์ที่ใส่ได้ → กดเลือก
3. ค่าพลังเพิ่มทันที (ดูได้ที่หน้าสเตตัส C)

ถอดออกที่แท็บ **จัดการการ์ด** → กด **ถอด** (การ์ดกลับเข้ากระเป๋า)
> อยากให้ถอดแล้วการ์ดหายแบบ RO ต้นฉบับ: แก้ `unsocket_card(..., return_to_inventory)` เป็น `false`
> ใน `scripts/core/player_state.gd`

### ช่องใส่การ์ดของอุปกรณ์
กำหนดที่ `data/items/<ชื่อ>.tres` → ช่อง **Card Slots** (0-4)

| อุปกรณ์ | ช่อง | อุปกรณ์ | ช่อง |
|---|---|---|---|
| ดาบมือใหม่ | 1 | เสื้อผ้าฝ้าย / ชุดนักผจญภัย | 1 |
| ฟัลชิออน / เบลด / เคลย์มอร์ | 2 | เกราะเพลท | 2 |
| ดาบเพลิง | 3 | การ์ด(โล่) / บัคเลอร์ | 1 |
| หมวก / ผ้าคลุม / รองเท้า / บูท | 1 | แหวน / คลิป / ถุงมือ | 1 |

### การ์ดทั้ง 11 ใบที่ใส่ไว้ให้ (แก้ได้ที่ `data/cards/`)

| การ์ด | ใส่ใน | คุณสมบัติ | ระดับ |
|---|---|---|---|
| โพริง | เครื่องประดับ | LUK +2, FLEE +5 | ธรรมดา |
| แฟเบร | ชุดเกราะ | VIT +1, MaxHP +100 | ธรรมดา |
| ลูนาติก | เครื่องประดับ | LUK +1, CRIT +3, FLEE +2 | ธรรมดา |
| ดรอปส์ | อาวุธ | DEX +1, HIT +3 | ไม่ธรรมดา |
| ชอนชอน | โล่ | AGI +1, DEF +1 | ไม่ธรรมดา |
| ฮอร์เน็ต | อาวุธ | STR +1, ATK +3 | ไม่ธรรมดา |
| หมาป่า | อาวุธ | ATK +15, HIT +5 | หายาก |
| มูนัค | ผ้าคลุม | MDEF +3, MaxHP +150 | หายาก |
| ออร์คนักรบ | อาวุธ | CRIT +5, **ATK +5%** | หายากมาก |
| บาฟโฟเมทจูเนียร์ | เครื่องประดับ | AGI +3, CRIT +7 | หายากมาก |
| บาฟโฟเมท | อาวุธ | STR +2, **ATK +10%, MaxHP +5%** | ตำนาน |

### เพิ่ม/แก้การ์ด
เปิดไฟล์ใน `data/cards/` แล้วแก้ใน Inspector ได้เลย:
- **Monster Id** — มอนเจ้าของการ์ด (ใช้ดึงรูปมาโชว์)
- **Fits Slot** — ใส่ในอุปกรณ์ช่องไหน
- **Illustration** — ★ ใส่ภาพการ์ดที่คุณวาดเองตรงนี้ ★
  (ถ้าเว้นว่าง ระบบจะใช้เฟรมแรกของอนิเมชัน Idle ของมอนตัวนั้นมาใส่ในกรอบให้เอง)
- **Rarity** 1-5 — เปลี่ยนสีกรอบการ์ด
- ค่าโบนัสตัวเลขตรง ๆ ใช้ช่องเดียวกับไอเทม (ATK, DEF, Bonus Str, Max Hp ...)
- **Percent Effects** — โบนัสแบบ % เช่น `{"atk_percent": 5.0}`
  รองรับ: `atk_percent, matk_percent, def_percent, max_hp_percent, max_sp_percent,`
  `aspd_percent, move_speed_percent, crit_damage_percent`
- **Effect Text** — เขียนคำอธิบายเอง (เว้นว่าง = ระบบสร้างให้อัตโนมัติจากค่าโบนัส)

**การ์ดมอนใหม่**: คลิกขวาใน `data/cards/` → New Resource → **CardData**
ตั้ง `id` เป็น `card_<ชื่อมอน>` แล้วเพิ่ม DropEntry 5% ในไฟล์มอนตัวนั้น

**เปลี่ยนอัตราดรอปทั้งหมด**: แก้ `chance` ของ DropEntry ที่ชี้ไปที่ `card_*` ในไฟล์มอนแต่ละตัว

---

## 7.6 ★ ตัวละครเปล่า + อาวุธโผล่ที่มือ (Paper Doll แบบ RO)

RO ทำแบบนี้: ตัวละครไม่ได้เป็นภาพเดียว แต่เป็น **หลายเลเยอร์ซ้อนกัน**
(ตัวเปล่า → เกราะ → โล่ → อาวุธ → หมวก) ทุกเลเยอร์เล่น **เฟรมเดียวกัน พร้อมกัน**
ระบบนี้ติดตั้งให้แล้วที่ `scripts/entities/character_visual.gd`
และเพิ่ม node `EquipVisual` ไว้ใน `scenes/player/player.tscn` เรียบร้อย

### ขั้นตอนทำ

**ขั้นที่ 1 — วาดตัวเปล่า**
วาดตัวละครแบบไม่มีอาวุธ/เกราะ ให้ครบทุกท่า (Idle, Run, Attack, ...)
แล้วเอาไปแทนที่ `data/sprites/player_frames.tres`
> ตอนนี้ `player_frames.tres` ยังเป็นตัวที่ **ถือดาบอยู่ในภาพ** (f1-f6)
> ต้องวาดใหม่แบบมือเปล่า ไม่งั้นจะเห็นดาบ 2 เล่ม

**ขั้นที่ 2 — วาดอาวุธเป็นเลเยอร์**
กฎเดียวที่ต้องจำ: **วาดบนผืนผ้าใบขนาดเท่ากันกับตัวเปล่า และเฟรมต้องตรงกัน**

| ตัวเปล่า | อาวุธ |
|---|---|
| Idle 6 เฟรม | Idle 6 เฟรม |
| Run 6 เฟรม | Run 6 เฟรม |
| Attack 6 เฟรม | Attack 6 เฟรม |

วิธีง่ายสุด: เปิดไฟล์ตัวเปล่าในโปรแกรมวาด → สร้างเลเยอร์ใหม่ทับ → วาดดาบให้อยู่ในมือของแต่ละเฟรม
→ ซ่อนเลเยอร์ตัวเปล่า → export เฉพาะเลเยอร์ดาบ (พื้นหลังโปร่ง ขนาดผ้าใบเท่าเดิม)
ได้ครบ 18 ภาพเหมือนกัน แค่มีแต่ดาบ

**ขั้นที่ 3 — ผูกเข้ากับไอเทม**
1. สร้าง `SpriteFrames` ใหม่จากภาพดาบ ตั้งชื่ออนิเมชันให้ตรง (Idle / Run / Attack)
   เซฟเป็น `data/sprites/sword_novice_frames.tres`
2. เปิด `data/items/novice_sword.tres` → กลุ่ม **ภาพตอนสวมใส่**
   → ลาก SpriteFrames ใส่ช่อง **Equip Sprite Frames**

จบ — ใส่ดาบเมื่อไหร่ ดาบโผล่ที่มือทันที ถอดออกก็หายไป ตอนโจมตีดาบจะฟันตามทุกเฟรม
เพราะ **ไม่ต้องตั้ง offset อะไรเลย** ถ้าผ้าใบขนาดเท่ากัน

### 📦 ไฟล์ตัวอย่างที่แถมมาให้ (ดูของจริงได้เลย)

```
Sprites/example/body/    ตัวเปล่า 18 ภาพ  (Idle/Run/Attack อย่างละ 6)
Sprites/example/sword/   ดาบ 18 ภาพ      (เฟรมตรงกันเป๊ะ)
data/sprites/example_body_frames.tres    SpriteFrames พร้อมใช้
data/sprites/example_sword_frames.tres
scenes/example/paperdoll_demo.tscn       ฉากสาธิต
ตัวอย่างสไปรท์.png                        ภาพรวมทั้ง 3 แถว
```

**ดูของจริง**: เปิด `scenes/example/paperdoll_demo.tscn` แล้วกด **F6**
จะเห็น 3 ตัวเทียบกัน — ตัวเปล่า / เลเยอร์ดาบ / ซ้อนกัน สลับท่าให้ดูอัตโนมัติ

**เอาไปลองในเกมจริง** (2 ช่อง):
1. `scenes/player/player.tscn` → `AnimatedSprite2D` → Sprite Frames = `example_body_frames.tres`
2. `data/items/novice_sword.tres` → Equip Sprite Frames = `example_sword_frames.tres`

กด F5 — เดิน วิ่ง ฟัน จะเห็นดาบอยู่ในมือตลอด ถอดดาบออกดาบก็หายไป
(ภาพตัวอย่างทั้งหมดสร้างด้วยโค้ด ใช้เป็นตัวเทียบระยะ/สัดส่วนได้ แล้วค่อยวาดของจริงทับ)

> สังเกตว่าไฟล์ดาบ **ไม่มีตัวละครอยู่ในภาพเลย** มีแต่ดาบลอย ๆ อยู่ตำแหน่งที่มือจะไปอยู่
> นั่นคือหัวใจของวิธีนี้

### ⚠ ข้อควรระวังจากสไปรท์ชุดปัจจุบันของคุณ
ตอนนี้ Idle/Run เป็น **50x90** แต่ Attack เป็น **80x90** — คนละขนาด
ระบบยังทำงานได้ แต่ต้องจำกฎนี้: **ภาพอาวุธเฟรมที่ N ต้องขนาดเท่ากับภาพตัวเฟรมที่ N**
(เพราะ Godot วางภาพโดยอิงจุดกึ่งกลางของแต่ละเฟรม)
ทางที่ปลอดภัยกว่าคือ **ทำทุกเฟรมให้ขนาดเดียวกันทั้งหมด** เหมือนไฟล์ตัวอย่าง

### ถ้ายังไม่อยากวาดครบทุกเฟรม (แบบ B)
ใส่ภาพดาบใบเดียวในช่อง **Equip Texture** แทน แล้วปรับ **Equip Offset** ให้ไปอยู่ตรงมือ
ดาบจะติดตัวไปด้วยแต่ไม่ขยับตามท่า — ใช้ดูคร่าว ๆ ก่อนได้ พอวาดครบแล้วค่อยย้ายไปช่อง A

### ช่องตั้งค่าใน ItemData (กลุ่ม "ภาพตอนสวมใส่")

| ช่อง | ทำอะไร |
|---|---|
| **Equip Sprite Frames** | ★ ภาพเคลื่อนไหวครบทุกท่า (แบบ A) |
| **Equip Texture** | ภาพนิ่งใบเดียว (แบบ B) |
| **Equip Offset** | เยื้องตำแหน่ง — ถ้าผ้าใบเท่าตัวเปล่า ปล่อย (0,0) |
| **Equip Z Index** | ลำดับวาด: บวก = อยู่หน้าตัว, ลบ = อยู่หลังตัว |
| **Use Flipped Z / Equip Z Index Flipped** | ให้ดาบอยู่หลังตัวตอนหันอีกด้าน (เหมือน RO) |

### ลำดับเลเยอร์ที่ตั้งไว้
ผ้าคลุม (หลังสุด) → รองเท้า → ชุดเกราะ → โล่ → อาวุธ → หมวก
แก้ได้ที่ `LAYER_ORDER` ใน `character_visual.gd` หรือตั้ง Equip Z Index รายชิ้น

### เกร็ด
- อุปกรณ์ที่มีแค่บางท่า (เช่นมีแต่ Attack) ระบบจะซ่อนเลเยอร์ตอนท่าอื่นให้เอง ไม่ค้าง
- เอาไปใช้กับ NPC/มอนได้: เพิ่ม node `CharacterVisual` แล้วปิด `Use Player Equipment`
  จากนั้นเรียก `set_layer(slot, item_data)` เอง
- เครื่องประดับไม่มีเลเยอร์ภาพ (ตามแบบ RO)

---

## 7.7 ★ ปรับขนาดตัวละคร

### แก้ที่ช่องเดียว
`scenes/player/player.tscn` → เลือก node **Player** → กลุ่ม **ขนาดตัวละคร** → **Auto Fit Height**

ค่านี้คือ "อยากให้ตัวละครสูงกี่พิกเซลบนจอ" ตอนนี้ตั้งไว้ **240**
- ใหญ่ไป → ลดเป็น 190-210
- เล็กไป → เพิ่มเป็น 280-320

**กล่องชนปรับตามให้อัตโนมัติแล้ว** (ช่อง Auto Fit Collision) แก้ตัวเลขเดียวจบ
อยากให้ตัวอ้วน/ผอมกว่านี้ ปรับ **Collision Width Ratio** (0.17 = ผอม, 0.25 = อ้วน)
- เทียบคร่าว ๆ: โพริงสูงประมาณ 60-70px ตัวละครควรสูงราว 2 เท่า

> ถ้าตั้ง **0** = ปิดระบบนี้ กลับไปใช้ช่อง **Scale** ของ `AnimatedSprite2D` เองเหมือนเดิม

### ทำไมต้องมีระบบนี้
ไฟล์ภาพของคุณตอนนี้ **ขนาดไม่เท่ากัน**:

| ท่า | ไฟล์ | ขนาดผ้าใบ |
|---|---|---|
| Idle | z1-z6 | **300 x 300** |
| Run | r1-r6 | 50 x 90 |
| Attack | f1-f6 | 80 x 90 |

ถ้าใช้ Scale ตัวเดียว (เดิม 1.92) จะได้ Idle สูง 576px แต่ Run สูงแค่ 173px
→ ตัวละครจะ **โตขึ้น 3 เท่าตอนยืน แล้วหดตอนวิ่ง**

ระบบใหม่จะวัด "ความสูงจริงของตัวละคร" ในแต่ละท่า (ไม่นับพื้นที่โปร่งใส)
แล้วคำนวณ Scale ให้เอง + จัดให้ **เท้าแตะพื้นระดับเดียวกัน** และ **ตัวอยู่กึ่งกลาง** ทุกท่า

ผลทดสอบจริงกับขนาดที่คุณใช้:

| ท่า | ผ้าใบ | ตัวสูงจริง | Scale ที่คำนวณได้ | สูงบนจอ |
|---|---|---|---|---|
| Idle | 300x300 | 240px | 0.583 | 140 |
| Run | 50x90 | 88px | 1.591 | 140 |
| Attack | 80x90 | 78px | 1.795 | 140 |

### กล่องชนไม่ต้องแตะแล้ว
ระบบสร้างแคปซูลใหม่ให้เองทุกครั้งที่เปลี่ยน Auto Fit Height
(สูง = Auto Fit Height, กว้าง = Auto Fit Height x Collision Width Ratio)

### ⚠ แต่ถ้าจะใช้ระบบซ้อนเลเยอร์อาวุธ (หัวข้อ 7.6)
ระบบปรับขนาดช่วยเรื่อง "ตัวใหญ่/เล็กไม่เท่ากัน" ได้ แต่ **ไม่ช่วยเรื่องอาวุธซ้อนตรง**
เพราะอาวุธต้องอิงผ้าใบเดียวกับตัว → สุดท้ายยังต้อง export ทุกท่าให้ผ้าใบขนาดเดียวกันอยู่ดี
แนะนำ: ทำใหม่ทั้งชุดที่ **300x300 เท่ากันหมด** แล้วตั้ง Auto Fit Height ให้ได้ขนาดที่ชอบ

---

## 7.8 ★ ท่าโจมตีแยกตามอาวุธ

ตอนนี้ท่า `Attack` ของคุณคือ **ต่อยมือเปล่า** ระบบเลยรองรับให้เพิ่มท่าถืออาวุธได้แล้ว
เวลาถืออาวุธ ตัวละครจะสลับไปเล่นท่าของอาวุธชนิดนั้นเอง ถอดอาวุธก็กลับมาต่อยมือเปล่า

### ชื่อท่าที่ต้องสร้าง (ตั้งใน SpriteFrames ของตัวละคร)

| ชื่อท่า | ใช้ตอนไหน | สถานะ |
|---|---|---|
| `Idle` | ยืน | ✅ มีแล้ว |
| `Run` | เดิน/วิ่ง | ✅ มีแล้ว |
| `Attack` | **โจมตีมือเปล่า** | ✅ มีแล้ว |
| **`Attack_Blade`** | **โจมตีตอนถือดาบมือใหม่/เบลด** | ⬅ สร้างอันนี้ |
| `Jump` `Hit` `Death` | กระโดด / เจ็บ / ตาย | ยังไม่มี (ระบบใช้ตัวสำรองไปก่อน) |

**ตั้งชื่อท่าให้อาวุธแต่ละชิ้นไว้แล้ว** — เปิดไฟล์ไอเทมดูได้ที่ช่อง **Attack Animation**

| อาวุธ | ไฟล์ | ชื่อท่าที่ต้องสร้าง |
|---|---|---|
| ดาบมือใหม่ | `novice_sword.tres` | **`Attack_Blade`** |
| เบลด | `blade.tres` | `Attack_Blade` (ใช้ท่าเดียวกัน) |
| ฟัลชิออน | `falchion.tres` | `Attack_Falchion` |
| เคลย์มอร์ | `claymore.tres` | `Attack_Claymore` |
| ดาบเพลิง | `flame_sword.tres` | `Attack_FlameSword` |

**อาวุธใหม่ที่จะทำเพิ่ม**: สร้างไฟล์ไอเทม → ใส่ชื่อท่าที่ช่อง **Attack Animation**
→ สร้างท่าชื่อเดียวกันใน SpriteFrames ตัวละคร → จบ ใส่อาวุธนั้นแล้วสลับท่าเอง
(ชื่อจะตั้งว่าอะไรก็ได้ ขอแค่ตรงกันทั้งสองที่)

### ลำดับการเลือกท่า
1. ชื่อในช่อง **Attack Animation** ของอาวุธที่ถือ → เช่น `Attack_Blade`
2. ถ้าไม่มีท่านั้น → `Attack_` + `weapon_type` → เช่น `Attack_sword`
3. ถ้ายังไม่มีอีก → `Attack` (มือเปล่า)

เพราะงั้นทำท่าไหนเสร็จก่อนก็ใช้ได้เลย ที่เหลือถอยไปใช้ท่าเดิมโดยไม่พัง

### สกิลก็ตั้งท่าเฉพาะได้
เปิดไฟล์สกิลใน `data/skills/` → ช่อง **Animation**
เช่นอยากให้ Magnum Break มีท่าหมุนตัวของมันเอง ก็ใส่ `Attack_magnum` แล้วสร้างท่าชื่อนั้น
เว้นว่าง = ใช้ท่าโจมตีตามอาวุธที่ถืออยู่

### ถ้ายังไม่ได้สร้างท่านั้น
ไม่พัง — ระบบจะไล่หาตามลำดับ: `ชื่อที่ขอ` → `Attack` → `Idle`
ทำสไปรท์เสร็จเมื่อไหร่ แค่เพิ่มท่าชื่อนั้นใน SpriteFrames มันจะใช้เองทันที ไม่ต้องแก้โค้ด

### ⚠ ถ้าใช้ระบบซ้อนเลเยอร์อาวุธ (หัวข้อ 7.6) ด้วย
ไฟล์ภาพอาวุธ (`Equip Sprite Frames`) ต้องมีท่า **ชื่อเดียวกัน** กับตัวละคร
คือต้องมี `Attack_Blade` ด้วย ไม่ใช่แค่ `Attack` ไม่งั้นตอนฟันดาบจะหายไป

---

## 7.9 ★ ตัวละครกับมอนสเตอร์ยืนระนาบเดียวกัน

### ปัญหาที่เจอ (แก้แล้ว)
เดิมระบบวัดระยะโจมตีจาก **จุดกำเนิด (origin)** ของแต่ละตัว
แต่จุดกำเนิดอยู่กึ่งกลางกล่องชน ซึ่งขนาดไม่เท่ากัน:

- ผู้เล่น กล่องชนสูง 240 → จุดกำเนิดอยู่เหนือเท้า **120px**
- โพริง กล่องชนสูง 50 → จุดกำเนิดอยู่เหนือเท้า **25px**

ยืนบนพื้นเดียวกันแท้ ๆ แต่จุดกำเนิดต่างกันถึง **95px**
ซึ่งเกินระยะโจมตีแนวตั้ง (`attack_range_y = 70`) → **ตีไม่โดนแม้ยืนติดกัน**

### แก้ยังไง
เปลี่ยนไปวัดจาก **ตำแหน่งเท้า** ทั้งคู่ (`foot_position()`)
เพราะ CharacterBody2D จะวางก้นกล่องชนแนบพื้นเสมอ → เท้าของทุกตัวอยู่ระนาบเดียวกันจริง ๆ

ผลทดสอบในแมพจริง:

| | จุดกำเนิด y | เท้า y | ต่างจากผู้เล่น |
|---|---|---|---|
| ผู้เล่น | 352.0 | 472.0 | – |
| โพริง | 446.9 | 471.9 | **0.1px** (เดิมต่าง 94.9px) |

ทั้งผู้เล่นตีมอน และมอนตีผู้เล่น ใช้ตำแหน่งเท้าเหมือนกันหมดแล้ว

### ภาพก็ยืนตรงระนาบด้วย
MonsterData เพิ่ม 2 ช่อง:

| ช่อง | ทำอะไร |
|---|---|
| **Display Height** | ความสูงของมอนบนจอ (พิกเซล) — 0 = ใช้ Sprite Scale เหมือนเดิม |
| **Align Feet** | จัดให้เท้าแตะพื้นเสมอ (เปิดไว้แล้ว) |

ตั้งค่าให้ทุกตัวแล้ว โดยอิงผู้เล่นสูง 240:

| มอน | สูง | มอน | สูง |
|---|---|---|---|
| แฟเบร / ชอนชอน | 72 | หมาป่า | 125 |
| ฮอร์เน็ต | 80 | มูนัค | 170 |
| ลูนาติก | 82 | ออร์คนักรบ | 205 |
| โพริง | 85 | บาฟโฟเมทจูเนียร์ | 150 |
| ดรอปส์ | 88 | **บาฟโฟเมท** | **340** |

เปลี่ยนขนาดผู้เล่นเมื่อไหร่ ปรับเลขพวกนี้ตามสัดส่วนได้เลย
(หรือใส่ 0 ถ้าอยากคุมด้วย Sprite Scale เองเหมือนเดิม)

---

## 7.10 ★ มอนเกิดกระจายทั่วแมพ (MapSpawner)

เปลี่ยนจากเดิมที่วางจุดเกิดทีละจุด มาเป็น **ตัวเดียวคุมทั้งแมพ**
node ชื่อ `Spawners/MapSpawner` ในแมพของคุณ

### ทำงานยังไง
1. อ่านขอบเขตแมพจาก `Map Bounds` เอง
2. แบ่งความกว้างแมพเป็นช่อง ๆ เท่าจำนวนมอน แล้วให้แต่ละตัวไปคนละช่อง
3. ยิงเรย์ลงหาพื้นในช่องนั้น → วางให้ **เท้าแตะพื้นพอดี**
4. ตายแล้วเกิดใหม่ **ที่จุดใหม่** ไม่ใช่ที่เดิม

ผลทดสอบในแมพจริง (โพริง 5 ตัว): กระจายกินพื้นที่ **743-928 px** ตัวที่ใกล้กันสุดห่าง **111-173 px**

### เพิ่มมอนชนิดใหม่เข้าแมพ
เลือก node **MapSpawner** → ช่อง **Monster Types** → กด `+` → ลากไฟล์ `.tres` ของมอนใส่
ใส่ได้เลย 3 ชนิดตามที่วางแผนไว้ → จะได้ 3 x 5 = **15 ตัวต่อแมพ**
(`Count Per Type` ตั้งไว้ 5 เปลี่ยนได้)

### ช่องตั้งค่าอื่น ๆ

| ช่อง | ทำอะไร |
|---|---|
| **Count Per Type** | เกิดชนิดละกี่ตัว (ตอนนี้ 5) |
| **Edge Margin** | เว้นจากขอบซ้าย-ขวาของแมพ (150) |
| **Min Spacing** | มอนต้องห่างกันอย่างน้อยกี่พิกเซล (170) |
| **Avoid Player Range** | ห้ามเกิดใกล้ผู้เล่นเกินระยะนี้ (280) |
| **Only When Player Exists** | เกิดเฉพาะตอนมีผู้เล่นในแมพ |
| **Spawn Offscreen** | ★ ให้เกิดนอกจอเท่านั้น (เปิดไว้) |
| **Offscreen Margin** | เผื่อจากขอบจอออกไปอีกกี่พิกเซล (120) |
| **Max Spawn Distance** | ไกลสุดที่ยอมให้เกิด กันไปเกิดคนละฟากแมพ (1600) |
| **Despawn Distance** | เดินห่างเกินเท่านี้ มอนจะถูกเก็บไปเกิดใหม่ข้างหน้า (2600) |
| **Spawn Interval** | ทยอยเกิดทีละตัว ห่างกันกี่วินาที (0.6) |
| **Relax After** | หาที่นอกจอไม่ได้กี่วินาทีถึงจะผ่อนเงื่อนไข (4) |

> ถ้าที่ว่างไม่พอ (แมพเล็ก/มอนเยอะ) ระบบจะค่อย ๆ ผ่อนเงื่อนไขให้เอง เพื่อให้เกิดครบจำนวน

### แมพอื่นที่ตั้งไว้ให้แล้ว
- **ป่ามืด** — ชอนชอน / ฮอร์เน็ต / หมาป่า อย่างละ 5 ตัว + บาฟโฟเมท (บอส) ยังใช้จุดเกิดตายตัวเหมือนเดิม
- **ทุ่งหญ้าตัวอย่าง** — โพริง / แฟเบร / ลูนาติก อย่างละ 5 ตัว

---

## 7.11 ★ ภาพไอเทม (ไอคอน + ตอนตกพื้น)

**ใส่ที่เดียว ใช้ได้ทุกที่** — ช่อง **Icon** ใน `ItemData` ใช้กับ:
กระเป๋า · ช่องสวมใส่ · ร้านค้า · **ของที่ตกอยู่บนพื้น** (ทั้งดรอปจากมอนและตอนผู้เล่นทิ้ง)

ดาบมือใหม่ใส่ให้แล้วที่ `Sprites/items/blade.png` (ตัดพื้นหลังดำออก ครอปชิดตัวดาบ ย่อเหลือ 141x256)

### เพิ่มไอคอนไอเทมอื่น
1. วางไฟล์ภาพใน `Sprites/items/`
2. เปิด `data/items/<ชื่อ>.tres` → ลากภาพใส่ช่อง **Icon**

**ไม่ต้องย่อภาพมาก่อน** — ระบบย่อให้เองตอนแสดงผล
ขนาดตอนตกพื้นคุมด้วยช่อง **Drop Display Size** (ตั้งไว้ 46 px ด้านที่ยาวที่สุด)
ตั้ง 0 = ใช้ขนาดจริงของไฟล์

> อยากให้ **เบลด** ใช้ภาพเดียวกันไปก่อน: เปิด `data/items/blade.tres` แล้วลาก `blade.png` ใส่ช่อง Icon

ของที่ตกพื้นจะลอยขึ้นลงเบา ๆ ให้สังเกตเห็นง่าย และมีชื่อไอเทมกำกับด้านบน

---

## 7.12 ★ แก้ 4 เรื่อง (รอบล่าสุด)

### 1) เดินทับแล้วเก็บของไม่ได้ — แก้แล้ว
คุณเดาถูกครับ **เป็นเรื่องความต่างระดับจริง ๆ**

จุดกำเนิด (origin) ของตัวละครอยู่ "กลางลำตัว" ซึ่งลอยสูงจากพื้น **120 px**
(เพราะกล่องชนสูง 240) แต่ของที่ตกอยู่วางที่ระดับ "พื้น"
โค้ดเดิมวัดระยะจากจุดกำเนิด → ของที่วางอยู่แทบเท้าจึงถูกมองว่าห่างเกิน 120 px เลยเก็บไม่ได้

แก้โดยเปลี่ยนไปวัดจาก **ปลายเท้า** ทั้งหมด (วิธีเดียวกับที่แก้เรื่องตีไม่โดน)

พร้อมกันนั้นเจออีกบั๊กที่ทำให้เก็บไม่ได้เหมือนกัน:
ของที่ดรอปจะ **ลอยค้างกลางอากาศ / ร่วงทะลุแมพ** เพราะคำนวณจังหวะหยุดผิด
ตอนนี้ของจะยิงเรย์หาพื้นระหว่างตกทุกเฟรม แล้ว **วางบนพื้นพอดี** เสมอ

ปรับระยะเก็บได้ที่ node `Player`:

| ช่อง | ค่า | ทำอะไร |
|---|---|---|
| **Pickup Range** | 90 | ระยะเก็บแนวนอน (กดปุ่ม F) |
| **Pickup Range Y** | 90 | ระยะเก็บแนวตั้ง เผื่อของอยู่ต่างระดับ |

และที่ `scenes/items/dropped_item.tscn`:
**Auto Pickup Range X** (70) / **Auto Pickup Range Y** (80) = ระยะเก็บอัตโนมัติตอนเดินผ่าน

### 2) หน้าสวมใส่ย่อภาพให้พอดี — แก้แล้ว
ช่องสวมใส่เดิมเอาไฟล์ภาพมาแปะขนาดจริง ภาพใหญ่ ๆ (ดาบ 141x256) เลยดันช่องจนบวม
ตอนนี้เปิด `expand_icon` + จำกัดความกว้างไอคอนไว้ **34 px**
→ ไฟล์ภาพจะใหญ่แค่ไหนก็ย่อลงพอดีช่องเสมอ ไม่ต้องย่อภาพมาก่อน

### 3) popup ลอยไปมุมบนซ้าย — แก้แล้ว
สาเหตุ: โค้ดเดิมสั่งจัดกึ่งกลางตอนสร้างกล่อง ซึ่งตอนนั้นกล่องยัง **กว้าง 0** อยู่
ค่าตำแหน่งเลยถูกล็อกไว้ผิดตั้งแต่แรก

เปลี่ยนโครงใหม่เป็น: กล่องกินเต็มจอ แล้วใช้ `CenterContainer` ให้เอนจินจัดกลางให้เอง
→ อยู่กลางจอเป๊ะทุกครั้ง ไม่ว่าข้อความจะสั้นยาวแค่ไหน หรือย่อ/ขยายหน้าต่างเกม
แถมใส่ **ฉากหลังมืดลง 45%** ให้กล่องเด่นขึ้นด้วย

### 4) มอนกระจุกอยู่ในจอ — แก้แล้ว
ตอนนี้มอน **เกิดนอกจอเท่านั้น** แล้วค่อย ๆ โผล่ทีละตัว (ห่างกัน 0.6 วินาที)
ตอนเดินเปิดแมพไป จะเจอมอนใหม่เรื่อย ๆ ไม่ใช่โผล่พรึบพร้อมกันต่อหน้า

- ระยะใกล้สุดคิดจาก **ครึ่งความกว้างจอ + 120 px** (จอ 1280 → ประมาณ 760 px)
- ไกลสุดไม่เกิน **1600 px** จะได้ไม่ไปเกิดคนละฟากแมพจนเดินหาไม่เจอ
- เดินห่างเกิน **2600 px** มอนตัวเก่าจะถูกเก็บไปเกิดใหม่ข้างหน้าแทน
- ระบบจะเลือก "ช่องที่ไกลผู้เล่นที่สุด" เสมอ
- ถ้าแมพแคบจนหาที่นอกจอไม่ได้ ระบบจะค่อย ๆ ผ่อนระยะลง (ภายใน 4 วินาที) เพื่อไม่ให้แมพว่างเปล่า
  แต่จะไม่มีทางเกิดใกล้กว่า 168 px จากตัวผู้เล่น

**ผลทดสอบในแมพจริงของคุณ** (โพริง 5 ตัว ผู้เล่นยืนนิ่งที่ x=60):
เกิดครบ 5 ตัวโดยใช้เวลาทยอยเกิด 9.3 วินาที · 3 ตัวแรกเกิดนอกจอ (607-873 px)
ตัวที่ใกล้ที่สุดยังห่าง 287 px · กระจายกินพื้นที่ 534 px

> **หมายเหตุ** แมพปัจจุบันของคุณกว้าง 1664 px แต่จอกว้าง 1280 px
> พื้นที่ "นอกจอ" จึงเหลือน้อยมาก ถ้าขยายแมพให้กว้างขึ้น (ดูหัวข้อ 3.5)
> ระบบนี้จะเห็นผลชัดกว่านี้เยอะ — มอนจะเกิดนอกจอได้ครบทุกตัว

---

## 7.13 ★ ปุ่ม / HUD / เซฟ / วาป (รอบล่าสุด)

### ⚠ ต้องปิด Godot แล้วเปิดใหม่ 1 ครั้ง
รอบนี้แก้ `project.godot` (เพิ่มปุ่มใหม่) — Godot จะอ่านค่าใหม่ตอนเปิดโปรเจกต์เท่านั้น
ไฟล์เดิมสำรองไว้ที่ `project_godot_ก่อนเพิ่มปุ่ม.txt`

### 1) ปุ่มเดินเป็น W A S D แล้ว

| ปุ่ม | ทำอะไร |
|---|---|
| **A / D** | เดินซ้าย-ขวา (ลูกศรซ้าย-ขวายังใช้ได้) |
| **W** | กระโดด (Space และลูกศรขึ้นก็ได้) |
| **S** | ลง (จองไว้ให้ระบบลงแท่นในอนาคต) |
| **J / X** | โจมตี |
| **F** | เก็บของ / คุยกับ NPC / เข้าประตู |
| **Q** | ยาแดง |
| **1-4** | สกิล |
| **Tab** | เมนูระบบ (เซฟ / โหลด / เริ่มใหม่) |

อยากเปลี่ยนปุ่มเอง: `Project → Project Settings → Input Map`

### 2) ปุ่มสกิล + ยา ย้ายขึ้นไปข้างหลอดเลือดแล้ว
แถวปุ่มลัดอยู่ **ขวามือของหลอด HP/SP/EXP ติดกันเลย** มุมซ้ายบน
มี 6 ปุ่ม: สกิล 1-4 · **ยาแดง (โชว์จำนวนที่เหลือ)** · **เมนู**

ปุ่มยาจะจางลงเมื่อยาหมด · ปุ่มสกิลจะจางตอนติดคูลดาวน์
แถวล่างจอเหลือแค่บรรทัดบอกปุ่ม

ปรับขนาดปุ่มได้ที่ `HOTKEY_SIZE` หัวไฟล์ `scripts/ui/hud.gd` (ตอนนี้ 54x46)

### 3) ตัวละครวิ่งไวขึ้น
`BASE_MOVE_SPEED` ใน `scripts/core/player_stats.gd`: **300 → 430**
อยากไวกว่านี้อีกก็แก้เลขนี้เลย (สกิล/ไอเทมที่เพิ่ม `move_speed_percent` จะคูณจากค่านี้)

### 4) เซฟเกม / เริ่มเกมใหม่
กด **Tab** (หรือปุ่ม "เมนู" ข้างหลอดเลือด) เปิดหน้าต่าง **เมนูระบบ**

- **3 ช่องเซฟ** แต่ละช่องมีปุ่ม บันทึก / โหลด / ลบ และโชว์เลเวลกับวันเวลาที่เซฟ
- **เริ่มเกมใหม่** — ล้างตัวละครกลับไปเป็น Lv.1 ของเริ่มต้นครบ เงิน 1000
  (ไฟล์เซฟที่บันทึกไว้ **ไม่หาย** ยังโหลดกลับมาได้)
- ปุ่มลัดเดิมยังใช้ได้: **F5** เซฟช่อง 1 · **F6** โหลดช่อง 1

ไฟล์เซฟอยู่ที่ `user://saves/slot_N.json`
(Windows: `%APPDATA%\Godot\app_userdata\<ชื่อโปรเจกต์>\saves`)

### 5) เมือง Asgard กว้างขึ้นเท่าภาพฉาก
ภาพ `Map_Asgard.png` (3212x941 ย่อ 1.1269) กินพื้นที่จริง **x -1036 ถึง 2584**
แต่พื้นเดินได้เดิมยาว x -1774..2395 (เลยขอบภาพทางซ้าย แต่ขาดไปทางขวา)

ตอนนี้ปรับให้ตรงกันเป๊ะ:

| ค่า | เดิม | ใหม่ |
|---|---|---|
| กล่องชนพื้น | 4169 x 48 | **3620 x 48** |
| ขอบเขตแมพ | Rect2(0, 0, 4000, 900) | **Rect2(-1036, -73, 3620, 1060)** |
| Auto Fit Bounds | เปิด | **ปิด** (ล็อกให้ตรงภาพ) |

ไฟล์เดิมสำรองไว้ที่ `scenes/maps/prontera_town_ก่อนขยาย.tscn.bak`

### 6) ของที่ดรอป ต้องกด F เก็บ
ไม่เก็บอัตโนมัติแล้ว — เดินเข้าไปใกล้จะมีป้าย **"กด F เก็บ"** ลอยขึ้นเหนือไอเทม

อยากให้กลับไปเก็บอัตโนมัติ: เปิด `scenes/items/dropped_item.tscn` → ติ๊ก **Auto Pickup**
ระยะที่ป้ายโผล่/เก็บได้ ปรับที่ **Auto Pickup Range X / Y** (70 / 80)

### 7) วาปไม่เด้ง popup แล้ว
เดินผ่านประตูเฉย ๆ = **ผ่านไปได้ตามปกติ ไม่เกิดอะไรขึ้น**
ยืนอยู่ในประตูจะมีป้าย **"กด F เพื่อไป ..."** ลอยอยู่ กดแล้วถึงจะเปลี่ยนแมพ

ถ้ายืนอยู่ตรงที่มีของตกอยู่ด้วย ปุ่ม F จะ **เก็บของก่อน** แล้วค่อยกดซ้ำเพื่อเข้าประตู

อยากได้กล่องยืนยันแบบเดิมเฉพาะประตูบางบาน: เลือก node ประตูนั้น → ติ๊ก **Require Confirm**
อยากให้เดินชนแล้วเข้าเลย: ติ๊ก **Auto Enter**

### 8) ท่าตายของมอนสเตอร์ — แก้แล้ว
**สาเหตุ:** โค้ดสั่งเล่นท่าชื่อ `Death` แต่ SpriteFrames ของโพริงตั้งชื่อว่า **`Die`**
ระบบหาไม่เจอเลยถอยไปเล่น `Idle` แทน — มอนจึงหายไปเฉย ๆ โดยไม่มีท่าตาย

**แก้ 3 อย่าง:**

1. **ชื่อท่าตั้งได้หลายแบบ** — `Death` / `Die` / `Dead` / `Dying` ใช้ได้หมด
   และ **ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ** (`die` = `Die` = `DIE`)
   ท่าอื่นก็เหมือนกัน: `Run`/`Walk`/`Move` · `Attack`/`Atk` · `Hit`/`Hurt`/`Damage` · `Jump`/`Hop`
2. **เล่นจนจบจริง** — เดิมตัดทิ้งที่ 0.6 วินาทีตายตัว ตอนนี้คำนวณจากจำนวนเฟรม ÷ ความเร็ว
   (ท่า Die ของโพริง 6 เฟรม 5 fps = 1.2 วินาที) แล้วค้างที่เฟรมสุดท้าย
3. **จางหายไปนุ่ม ๆ** แทนที่จะหายวับ

ปรับได้ใน MonsterData หมวด **ท่าตาย**:

| ช่อง | ทำอะไร |
|---|---|
| **Death Time** | บังคับให้ท่าตายยาวกี่วินาที (0 = คิดจากจำนวนเฟรมเอง) |
| **Death Fade** | เล่นจบแล้วจางหายกี่วินาที (0.35) |

> มอนที่ตายแล้วจะไม่ถูกนับเป็น "ตัวเป็น" อีก ระบบเกิดมอนจึงเติมตัวใหม่ได้ทันที
> ไม่ต้องรอศพหายก่อน

---

## 7.14 ★ รูปการ์ดกึ่งกลาง + popup ตอนได้การ์ดใบใหม่

### 1) รูปในช่องไอเทม/การ์ดอยู่กึ่งกลางแล้ว
**สาเหตุเดิม:** ใช้ `Button.icon` + `expand_icon` ซึ่ง Godot จะวางรูป **ชิดมุม** ไม่ใช่กึ่งกลาง
โดยเฉพาะภาพทรงสูง (การ์ด) จะเห็นชัดว่าเบี้ยวไปทางซ้าย

**แก้:** เลิกใช้ `Button.icon` เปลี่ยนเป็น `TextureRect` ซ้อนเต็มปุ่มแทน
ตั้ง `STRETCH_KEEP_ASPECT_CENTERED` → **กึ่งกลางเป๊ะ + ไม่บิดสัดส่วน** ไม่ว่าภาพจะขนาดไหน

ใช้กับ **กระเป๋า · ช่องสวมใส่ · อัลบั้มการ์ด** ทั้งหมด
(ช่องสวมใส่จัดรูปชิดซ้าย-กึ่งกลางแนวตั้ง เพื่อเว้นที่ให้ชื่อไอเทม)

ตัวเลขที่วัดได้จริงจากการทดสอบ: กึ่งกลางปุ่มกับกึ่งกลางรูปตรงกัน **คลาดเคลื่อน 0.0 px**

มีฟังก์ชันกลางให้เรียกใช้ซ้ำแล้วที่ `UITheme.make_slot_icon(button)`
คืน `[TextureRect (รูป), Label (จำนวน มุมขวาล่าง)]`

> **การ์ดที่ยังไม่ได้ใส่ไอคอนเอง** จะดึงภาพการ์ด (กรอบ + รูปมอน) มาโชว์ในกระเป๋าให้อัตโนมัติ

### 2) ได้การ์ดใบใหม่ครั้งแรก → เด้ง popup ยินดีด้วย
เก็บการ์ดที่ **ยังไม่เคยมี** ปุ๊บ จะเด้งกล่องกลางจอ:

```
        ยินดีด้วยนักผจญภัย!
   ┌────────────────────────┐
   │   [ ภาพการ์ดใบเต็ม ]    │
   └────────────────────────┘
      คุณได้การ์ด
      [ การ์ดโพริง ]
      กดปุ่มใดก็ได้เพื่อปิด
```

- โชว์ **การ์ดใบเต็ม** (กรอบสีตามระดับความหายาก + รูปมอนสเตอร์ + คุณสมบัติ)
- ชื่อการ์ดเปลี่ยนสีตามระดับความหายากของใบนั้น
- **หยุดเกมไว้ให้ดู** ปิดด้วยการกดปุ่มใดก็ได้ / คลิก / หรือรอ 6 วินาที
- **ใบซ้ำไม่เด้งอีก** — เด้งเฉพาะครั้งแรกของการ์ดใบนั้น ๆ
- นับรวมการ์ดที่ใส่อยู่ในอุปกรณ์ด้วย (ถอดออกแล้วเก็บใหม่ก็ไม่เด้งซ้ำ)

ปรับเวลาปิดเองได้ที่ `auto_close` ในไฟล์ `scripts/ui/card_get_popup.gd` (0 = ไม่ปิดเอง)

### เบื้องหลัง
- สัญญาณใหม่ `Events.card_obtained(card_id)`
- **ของทุกชิ้นเข้ากระเป๋าผ่าน `PlayerState.gain_item(inst)` แล้ว** (แทน `inventory.add()` ตรง ๆ)
  ตัวนี้จะเช็คก่อนใส่ว่า "เป็นการ์ดที่ยังไม่เคยมีหรือเปล่า" แล้วยิงสัญญาณให้
  **ถ้าเขียนโค้ดให้ของเข้ากระเป๋าเพิ่มในอนาคต ให้เรียก `gain_item()` เสมอ** ไม่งั้น popup จะไม่เด้ง

---

## 7.15 ★ ท่าสกิลแยกตามอาวุธ + ไอคอนสกิล

### 1) ท่าสกิลแยกตามอาวุธ — เชื่อม `Attack_Blade_bash` ให้แล้ว

ระบบไล่หาชื่อท่าตามลำดับนี้ เจอชื่อไหนก่อนก็ใช้อันนั้น:

| ลำดับ | ชื่อท่า | ใช้เมื่อ |
|---|---|---|
| 1 | ช่อง **Animation** ใน SkillData | ตั้งชื่อท่าเองในไฟล์สกิล (ปกติเว้นว่าง) |
| 2 | **`Attack_<อาวุธ>_<สกิล>`** | ★ ท่าเฉพาะอาวุธนั้น + สกิลนั้น |
| 3 | `Attack_<สกิล>` | ท่าสกิลกลาง ใช้ได้กับทุกอาวุธ |
| 4 | `Attack_<อาวุธ>` | ยังไม่มีท่าสกิล ใช้ท่าโจมตีปกติของอาวุธ |
| 5 | `Attack` | มือเปล่า |

**ตอนนี้ใช้ได้แล้ว:** ถือ *ดาบมือใหม่* (ท่าโจมตี `Attack_Blade`) กดสกิล *bash*
→ เล่นท่า **`Attack_Blade_bash`** อัตโนมัติ

### ทำอาวุธชิ้นใหม่ต้องทำอะไรบ้าง
1. วาดท่าโจมตีปกติ ตั้งชื่อเช่น **`Attack_Claymore`**
2. วาดท่าสกิล ตั้งชื่อ **`Attack_Claymore_bash`** (ต่อท้ายด้วย id ของสกิล)
3. เปิด `data/items/<อาวุธ>.tres` → ช่อง **Attack Animation** ใส่ `Attack_Claymore`

**แค่นี้ ไม่ต้องแก้โค้ดเลย** — id ของสกิลดูได้จากช่อง **ID** ในไฟล์ `data/skills/*.tres`
(`bash` · `magnum_break` · `sword_mastery` · `endure` · `battle_cry` · `first_aid` · `hp_recovery`)

> **ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ** — `attack_blade_bash` ก็หาเจอ
> อยากเปลี่ยนรูปแบบชื่อ: node `Player` → กลุ่ม **ท่าโจมตี** →
> **Skill Weapon Anim Format** (`{attack}_{skill}`) · **Skill Anim Format** (`Attack_{skill}`)

### อยากให้ท่าสกิลใช้ร่วมกันทุกอาวุธ
วาดท่าเดียวชื่อ **`Attack_bash`** — อาวุธไหนที่ยังไม่มีท่าเฉพาะจะมาใช้ท่านี้แทน

### 2) ไอคอนสกิล — โชว์แล้วทั้ง 2 ที่

ใส่ภาพในช่อง **Icon** ของ `data/skills/<สกิล>.tres` ที่เดียว ใช้ได้ทั้ง:

- **ปุ่มลัด 1-4 ข้างหลอดเลือด** — โชว์รูปเต็มปุ่ม มีเลขปุ่มลัดมุมซ้ายบน
  ชี้เมาส์ค้างจะเห็นชื่อ + คำอธิบายสกิล · ปุ่มจะจางลงตอนติดคูลดาวน์เหมือนเดิม
- **หน้าต่างสกิล (K)** — โชว์ไอคอนหน้าชื่อสกิลทุกแถว
  สกิลที่ยังไม่ได้เรียนไอคอนจะจาง ๆ

**สกิลที่ยังไม่ได้ใส่ไอคอน** ปุ่มลัดจะโชว์ชื่อสกิลย่อ ๆ แทน (เหมือนเดิม) ไม่พัง

ภาพจะถูก **ย่อให้พอดีช่องและจัดกึ่งกลางเอง** ไม่ว่าไฟล์ต้นฉบับจะขนาดไหน
ปรับขนาดไอคอนในหน้าต่างสกิลได้ที่ `ICON_SIZE` หัวไฟล์ `scripts/ui/skill_window.gd` (34 px)

> ตอนนี้ใส่ไอคอนแล้ว 1 สกิล (**bash**) เหลืออีก 6 สกิล ใส่เพิ่มได้เลย

---

## 7.16 ★ กล่องรายละเอียดไอเทม (เด้งข้างหน้าต่าง)

กดที่ไอเทม → กล่องรายละเอียดโผล่ **ข้าง ๆ หน้าต่างที่กดมา** ไม่ทับกัน แบบเกม Ragnarok

```
┌─ กระเป๋า ──────┐ ┌─ ดาบมือใหม่ ────── ✕ ─┐
│ ▣ ▣ ▣ ▣ ▣ ▣ │ │ ┌──────┐ ประเภท : อาวุธ │
│ ▣ ▣ ▣ ▣ ▣ ▣ │ │ │ รูป  │ พลังโจมตี : 25  │
│ ▣ ▣ ▣ ▣ ▣ ▣ │ │ │ ใหญ่ │ ตีบวก : +0 / +10│
│              │ │ └──────┘ ช่องการ์ด : 0/1 │
└──────────────┘ └────────────────────────┘
```

### ใช้ได้ที่ไหนบ้าง
| หน้าต่าง | กดตรงไหน | ได้อะไรเพิ่ม |
|---|---|---|
| **กระเป๋า (I)** | กดช่องไอเทม | ค่าตีบวกจริง + การ์ดที่ใส่อยู่ |
| **ร้านค้า — แท็บซื้อ** | กดชื่อของ | บอก **ราคาซื้อ** ด้านบน |
| **ร้านค้า — แท็บขาย** | กดชื่อของ | บอก **ราคาขาย** ที่จะได้จริง |
| **ช่องสวมใส่ (E)** | กดช่อง | กดครั้งแรก = ดูรายละเอียด · **กดซ้ำ = ถอดออก** |

### ในกล่องมีอะไร
รูปไอเทมใหญ่ 120x120 · ประเภท (อาวุธ/ชุดเกราะ/ของสวมศีรษะ/การ์ด/ของใช้/วัตถุดิบ) ·
พลังโจมตี-ป้องกัน · ค่าสเตตัสที่เพิ่มให้ · ระดับตีบวก · ช่องการ์ดและการ์ดที่ใส่อยู่ ·
คุณสมบัติการ์ด · คำอธิบายไอเทม · ราคา

- ของยาว ๆ จะมี **แถบเลื่อน** ให้อ่านต่อ (สูงสุด 280 px แล้วค่อยเลื่อน)
- ถ้าวางด้านขวาแล้วล้นจอ จะ **ย้ายไปด้านซ้ายเอง** และไม่หลุดขอบจอ
- **ลากหน้าต่างไปไหน กล่องตามไปด้วย** · ปิดหน้าต่างต้นทาง กล่องหายตาม
- ปิดกล่องได้ที่ปุ่ม ✕ หรือกด Esc

### ปรับแต่ง
หัวไฟล์ `scripts/ui/item_info_popup.gd`:

| ค่า | ทำอะไร |
|---|---|
| `ART_SIZE` | ขนาดกรอบรูป (120x120) |
| `BOX_WIDTH` | ความกว้างกล่อง (380) |
| `MAX_HEIGHT` | สูงเท่าไหร่ถึงจะมีแถบเลื่อน (280) |
| `GAP` | ห่างจากหน้าต่างต้นทางกี่พิกเซล (8) |

**เรียกใช้จากที่อื่น** (เผื่ออยากเพิ่มในหน้าต่างใหม่):
```gdscript
UI.show_item(inst, self)                       # ของในกระเป๋า
UI.show_item_data(item_data, self, "ข้อความ")   # แม่แบบไอเทม เช่นในร้าน
UI.hide_item_popup()
```

---

## 7.17 ★ แก้อัตรา miss (อัพ DEX แล้วคุ้มขึ้น)

### ปัญหาเดิม
สูตรเก่า: `โอกาสเข้า % = 75 + HIT ผู้เล่น − FLEE มอน` และ **DEX 1 แต้ม = HIT +1 เท่านั้น**
Lv1 DEX1 ตีโพริงเข้าแค่ **74%** (พลาด 1 ใน 4 ครั้ง) และอัพ DEX 20 แต้มได้ความแม่นเพิ่มแค่ 20%
ขณะที่ FLEE ของมอนโตเร็วกว่า (ประมาณ เลเวล x1.5) — ยิ่งไปแมพยาก ยิ่ง miss

### แก้ 3 จุด
| ค่า | เดิม | ใหม่ | อยู่ที่ |
|---|---|---|---|
| ฐานตอน **ผู้เล่นตีมอน** | 75 | **85** | `BASE_HIT_RATE` |
| ฐานตอน **มอนตีผู้เล่น** | 75 (ใช้ร่วมกัน) | **72** (แยกออกมา) | `MONSTER_BASE_HIT_RATE` |
| **DEX 1 แต้ม** | HIT +1 | **HIT +1.5** | `HIT_PER_DEX` |
| เพดานความแม่น | 95% | **98%** | `MAX_HIT_RATE` |

**แยกฐานของสองฝั่งออกจากกัน** — ผู้เล่นแม่นขึ้นโดยที่มอนไม่ได้แม่นขึ้นตาม
(โพริงตีผู้เล่น Lv10 AGI10: เดิม 62% → ใหม่ **59%** คือมอนพลาดมากขึ้นนิดหน่อยด้วย)

### ผลจริงหลังแก้ (โอกาสตีเข้า %)

| ผู้เล่น | โพริง | แฟเบร | ลูนาติก | ชอนชอน | ฮอร์เน็ต | หมาป่า | มูนัค | ออร์ค |
|---|---|---|---|---|---|---|---|---|
| Lv1 DEX1 | 84 | 81 | 71 | 60 | 52 | 54 | 47 | 43 |
| Lv5 DEX10 | 98 | 98 | 89 | 78 | 70 | 72 | 65 | 61 |
| Lv10 DEX20 | 98 | 98 | 98 | 98 | 90 | 92 | 85 | 81 |
| Lv20 DEX40 | 98 | 98 | 98 | 98 | 98 | 98 | 98 | 98 |

- Lv1 ตีโพริง: 74% → **84%**
- Lv10 ตีฮอร์เน็ต: DEX1 = 61% → **อัพ DEX เป็น 21 ได้ 91%** (อัพ 20 แต้ม = +30% ไม่ใช่ +20% แบบเดิม)
- **บาฟโฟเมท (บอส)** Lv20 DEX40 ยังเข้าแค่ 55% — บอสยังต้องเตรียมตัว

ทดสอบสุ่มตีจริง 4,000 ครั้ง: เข้า 90.7% ตรงกับที่สูตรคำนวณไว้ (90%)

### อยากปรับเอง
เปิด `scripts/core/combat.gd` หัวไฟล์ — แก้ `BASE_HIT_RATE` ตัวเดียวก็เปลี่ยนทั้งเกม
(+5 = ตีเข้ามากขึ้น 5% กับมอนทุกตัว)
อยากให้ DEX คุ้มกว่านี้อีก แก้ `HIT_PER_DEX` ใน `scripts/core/player_stats.gd`

> **ถ้ามอนตัวไหนหลบเก่งเกินไปเป็นตัว ๆ** แก้ช่อง **Flee** ในไฟล์ `data/monsters/<ตัวนั้น>.tres`
> ได้โดยตรง (ค่านี้เป็น "โบนัส" ระบบบวก 100 + เลเวล ให้เอง)

---

## 7.18 ★ เปลี่ยนอาวุธแล้วภาพตัวละครเปลี่ยนตาม "ทุกท่า"

### ทำไม falchion ใส่แล้วยังเป็นภาพเดิม
เพราะเมื่อก่อน **มีแค่ท่าโจมตี** ที่แยกตามอาวุธ
ท่ายืน (`Idle`) กับท่าวิ่ง (`Run`) มีอันเดียว — และในภาพนั้นตัวละคร **ถือดาบเล่มเดิมอยู่แล้ว**
เวลาใส่ falchion จึงเห็นดาบเก่าตลอด ยกเว้นตอนกดโจมตีแวบเดียว

### ตอนนี้ทุกท่าแยกตามอาวุธได้หมดแล้ว
ตั้งชื่อว่า **`<ท่า>_<ชื่ออาวุธ>`** ระบบจะหยิบมาใช้ก่อนเสมอ ถ้าไม่มีก็ถอยไปใช้ท่าธรรมดา

| อยากให้เปลี่ยนตอนไหน | ตั้งชื่อท่าว่า |
|---|---|
| ยืนเฉย ๆ | `Idle_falchion` |
| วิ่ง | `Run_falchion` |
| กระโดด | `Jump_falchion` |
| โจมตีปกติ | `Attack_falchion` |
| ใช้สกิล bash | `Attack_falchion_bash` |
| โดนตี | `Hit_falchion` |
| ตาย | `Death_falchion` |

**วาดเฉพาะท่าที่อยากให้เปลี่ยนก็พอ** ท่าที่ไม่ได้วาดจะใช้ภาพธรรมดาไปก่อน ไม่พัง

> ตอนนี้ falchion มีแค่ `Attack_falchion` กับ `Attack_falchion_bash`
> ถ้าอยากให้เห็นดาบใหม่ตลอดเวลา ต้องวาด **`Idle_falchion`** กับ **`Run_falchion`** เพิ่ม

### "ชื่ออาวุธ" เอามาจากไหน
1. ช่อง **Attack Animation** ของไอเทม — `Attack_Falchion` → ชื่ออาวุธคือ `Falchion`
2. ถ้าเว้นว่างไว้ → ใช้ **id ของไอเทม** เช่น `falchion`

**ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ** — ตั้ง `Attack_Falchion` ในไอเทม แต่วาดสไปรท์ชื่อ `Attack_falchion` ก็เจอ

### เพิ่มอาวุธใหม่ = ไม่ต้องตั้งค่าอะไรเลย
ตั้งชื่อท่าให้ตรงกับ **id ของไอเทม** ก็พอ เช่นไอเทม id `claymore` วาดท่า `Attack_claymore`
ระบบเดาให้เอง ไม่ต้องแตะช่อง Attack Animation

### ⚠ ข้อสังเกตของ falchion.tres
ไฟล์นี้ตั้งช่อง **Equip Sprite Frames** ไว้ด้วย โดยข้างในมีท่าชื่อ **`Attack falchion` (เว้นวรรค)**

ช่องนี้คือระบบ **Paper Doll** (ซ้อนภาพอาวุธทับตัวเปล่า) ซึ่งต้องตั้งชื่อท่า **ให้ตรงกับท่าของตัวละคร**
ชื่อที่มีเว้นวรรคจึงไม่ตรงกับอะไรเลย → เลเยอร์นี้ถูกซ่อนอยู่ ไม่มีผลอะไรตอนนี้

**แนะนำ:** ภาพ `Attack falchion.png` ถูกใช้เป็นท่าของตัวละครโดยตรงอยู่แล้ว (ใน `player_frames.tres`)
จึง **ล้างช่อง Equip Sprite Frames ของ falchion ทิ้งได้เลย** จะได้ไม่วาดซ้อนกันสองชั้นในอนาคต
(ถ้าจะใช้ Paper Doll จริง ๆ ต้องวาดเฉพาะ "ตัวดาบ" ไม่ใช่ทั้งตัวละคร แล้วตั้งชื่อท่าให้ตรงกัน)

---

## 7.19 ★ เลเวลของสวมใส่ · ช่องการ์ดเฉพาะของดรอป · ระบบเควส

### ⚠ ต้องปิด Godot แล้วเปิดใหม่ 1 ครั้ง (เพิ่มปุ่ม U)

---

### 1) ของสวมใส่มีระดับเลเวลแล้ว

ช่อง **Required Level** ใน `data/items/<ไอเทม>.tres` — เลเวลไม่ถึงใส่ไม่ได้ มีข้อความบอก
กล่องรายละเอียดไอเทมจะโชว์ **เขียว = ใส่ได้ / แดง = ยังไม่ถึง**

| ไอเทม | Lv | | ไอเทม | Lv |
|---|---|---|---|---|
| ดาบมือใหม่ · เสื้อผ้าฝ้าย · การ์ด · หมวก · รองเท้าแตะ | 1 | | ผ้าคลุมนักผจญภัย | 8 |
| เบลด · ฮู้ด | 4 | | ถุงมือ | 10 |
| ฟัลชิออน · บักเลอร์ | 12 | | รองเท้าบู๊ต | 15 |
| แหวน | 18 | | เคลย์มอร์ | 25 |
| เกราะเพลท | 30 | | ดาบเพลิง | 40 |

**เปลี่ยนเลขได้เลย** ที่ช่อง Required Level ของไอเทมนั้น ๆ

---

### 2) ★ ช่องการ์ดติดมากับ "ของที่ดรอปจากมอน" เท่านั้น ★

| ที่มาของของ | ช่องการ์ด |
|---|---|
| **ซื้อจากร้านค้า** | ไม่มี (0 ช่อง) |
| **ของแจกตอนเริ่มเกม** | ไม่มี |
| **ดรอปจากมอนสเตอร์** | มี ตามที่ตั้งไว้ในช่อง Card Slots |

ช่อง **Card Slots** ใน ItemData ตอนนี้แปลว่า **"จำนวนช่องสูงสุดถ้าดรอปจากมอน"**
ส่วนช่องจริงของแต่ละชิ้นเก็บไว้ที่ `ItemInstance.slots` (แยกกันทีละชิ้น)

- ชื่อไอเทมจะมี `[2]` ต่อท้ายเฉพาะชิ้นที่มีช่องจริง ๆ
- กล่องรายละเอียดบอกชัดเจน: *"ช่องการ์ด: ไม่มี (ของจากร้านค้าไม่มีช่อง)"*
- ในร้านค้าจะบอกว่า *"สูงสุด 2 ช่อง ถ้าดรอปจากมอนสเตอร์"*

> **แก้บั๊กเก่าที่เจอระหว่างทำ:** ตอนเก็บของสวมใส่เข้ากระเป๋า ระบบสร้างชิ้นใหม่ทับ
> ทำให้ **การ์ดที่ใส่ไว้และค่าตีบวกหายไป** ตอนนี้เก็บ "ชิ้นเดิม" แล้ว ไม่หายอีก

---

### 3) ★ ระบบเควส ★ (กด **U** เปิดสมุดเควส)

**เควสที่มีตอนนี้**

| เควส | คนให้ | เงื่อนไข | รางวัล |
|---|---|---|---|
| กำจัดโพริงให้ฮันส์ | ช่างตีเหล็กฮันส์ | ล่าโพริง 100 ตัว | **ฟราคอน x3** + EXP 500 |
| กำจัดแฟเบรให้โทนี่ | พ่อค้าโทนี่ | ล่าแฟเบร 100 ตัว | **3,000 z** + EXP 800 |

**วิธีเล่น**
1. NPC ที่มีเควสจะมีเครื่องหมาย **`!` สีเหลือง** ลอยเหนือหัว
2. กด **F** คุย → กล่องถามว่ารับเควสไหม
3. ออกไปล่ามอน — มีข้อความบอกความคืบหน้าทุก 10 ตัว
4. ครบแล้วเครื่องหมายเปลี่ยนเป็น **`?` สีเขียว** → กลับไปคุยเพื่อรับรางวัล

**สมุดเควส (U)** โชว์เควสที่กำลังทำ แถบความคืบหน้า และเควสที่ทำเสร็จแล้ว
เควสจะถูกเซฟไปกับไฟล์เซฟด้วย

> **NPC ที่มีเควสยังใช้งานปกติ** — คุยเรื่องเควสจบแล้ว ร้านค้า/หน้าตีบวกจะเปิดตามเดิม

### เพิ่มเควสใหม่ (ไม่ต้องเขียนโค้ด)
1. ก็อปไฟล์ `data/quests/hans_poring.tres` ตั้งชื่อใหม่
2. แก้ `id`, `title`, `kill_monster_id`, `kill_count`, รางวัล
3. เอา `id` ไปใส่ช่อง **Quest Ids** ของ NPC คนที่จะให้เควส

| ช่องใน QuestData | ทำอะไร |
|---|---|
| **Kill Monster Id / Kill Count** | ต้องล่ามอนตัวไหน กี่ตัว (เว้นว่าง = แค่ไปคุย) |
| **Required Level** | ต้องเลเวลเท่าไหร่ถึงรับได้ |
| **Reward Item Id / Count** | ของรางวัล |
| **Reward Zeny / Exp** | เงินและค่าประสบการณ์ |
| **Repeatable** | ทำซ้ำได้ไหม |
| **Dialog Offer / Progress / Complete** | บทพูด 3 ช่วง |

> กระเป๋าเต็มจะส่งเควสไม่ได้ (มีข้อความเตือน) ของรางวัลจะไม่หาย

---

## 8. ยังไม่ได้ทำ

ระบบเควส, กิลด์นักผจญภัย, อาชีพที่ 2 (Knight — โครงรองรับไว้แล้ว), สกิลระยะไกล, เสียง/เพลง

---

## 7.20 ปุ่มเมาส์ — คลิกซ้ายตีธรรมดา / คลิกขวาใช้สกิลช่อง 1

### ใช้ยังไง
| ปุ่ม | ทำอะไร |
|---|---|
| **คลิกซ้าย** | โจมตีปกติ (เหมือนกด J หรือ X) |
| **คลิกขวา** | ใช้สกิลที่ตั้งไว้ใน **ช่องลัดที่ 1** (เหมือนกดเลข 1) |

- คลิกแล้ว **ตัวละครจะหันไปทางที่คลิกให้เอง** ก่อนฟัน (คลิกซ้ายของตัว = หันซ้าย, คลิกขวาของตัว = หันขวา)
- ปุ่มคีย์บอร์ดเดิมยังใช้ได้ครบเหมือนเดิม ไม่ได้ตัดอะไรทิ้ง
- **ไม่ต้องรีสตาร์ท Godot** รอบนี้ไม่ได้แตะ `project.godot` เลย

### กันคลิกโดน UI แล้วฟันลม
คลิกในหน้าต่างกระเป๋า / ร้านค้า / สมุดเควส / กล่องยืนยัน หรือคลิกบนแผงหลอดเลือด-ปุ่มลัด
**จะไม่ทำให้ตัวละครฟัน** กันไว้ 2 ชั้น:

1. คลิกรับที่ `Player._unhandled_input()` — ถ้าปุ่ม/หน้าต่างบนจอกินคลิกไปแล้ว คลิกจะไม่ไหลมาถึงตัวละคร
2. เช็คซ้ำด้วย `UI.is_point_over_ui(จุดที่คลิก)` — ทับกรอบหน้าต่างที่เปิดอยู่เมื่อไหร่ ตัดทิ้งทันที

เปิดกระเป๋าค้างไว้แล้วคลิก **นอก** หน้าต่าง ยังฟันได้ปกติ

### ปรับได้ใน Inspector (เลือกโหนด Player)
กลุ่ม **"ปุ่มเมาส์"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| Mouse Attack | เปิด | คลิกซ้าย = โจมตีปกติ (ปิดได้ถ้าไม่ชอบ) |
| Mouse Skill Slot | 1 | คลิกขวาใช้สกิลช่องลัดที่เท่าไร (1-4) · ใส่ **0 = ปิดคลิกขวา** |
| Mouse Turns Facing | เปิด | คลิกแล้วหันหน้าไปทางที่คลิก |

> อยากให้คลิกขวาเป็นสกิลช่อง 2 ก็แค่เปลี่ยน **Mouse Skill Slot** เป็น 2 ไม่ต้องแก้โค้ด

### ไฟล์ที่แก้
- `scripts/entities/player.gd` — รับคลิกที่ `_unhandled_input()` + `_face_click()` + ตัวเลือก 3 ช่อง
- `scripts/ui/ui_manager.gd` — เพิ่ม `is_point_over_ui()`
- `scripts/ui/hud.gd` — เก็บ `top_panel` / `bottom_panel` ไว้เช็คคลิก + แถบคำใบ้บอกเรื่องเมาส์
- `scripts/core/input_setup.gd` — เลิกผูกปุ่มเมาส์ไว้ใน InputMap (ถ้าผูกไว้ คลิกโดนหน้าต่างจะกลายเป็นฟันลม)

### ข้อควรระวัง (บทเรียนจากรอบนี้)
- **ห้ามผูกปุ่มเมาส์ไว้ใน InputMap** แล้วอ่านด้วย `Input.is_action_just_pressed()`
  เพราะวิธีนั้นไม่สนใจว่า UI กินคลิกไปแล้วหรือยัง — คลิกปุ่มในกระเป๋าก็จะฟันลมไปด้วย
  ให้รับที่ `_unhandled_input()` แทนเสมอ
- ตำแหน่งที่คลิกในโลก ให้แปลงจาก `event.position` ด้วย
  `get_viewport().get_canvas_transform().affine_inverse() * event.position`
  (ใช้ `get_global_mouse_position()` ตรง ๆ จะทดสอบไม่ได้ และไม่ตรงกับคลิกที่เพิ่งเกิด)
- โหมด headless **ไม่มีระบบชี้เมาส์ของ GUI** — `gui_get_hovered_control()` คืน null เสมอ
  เทสต์จึงต้องเช็คด้วยกรอบหน้าต่าง (`get_global_rect()`) แทน
  และตอน `push_input()` ต้องส่ง `in_local_coords = true` ไม่งั้นพิกัดจะเพี้ยน

---

## 7.21 หน้าสวมใส่อุปกรณ์ — จัดใหม่ให้เหมือนหน้าต่างของ RO

### หน้าตาใหม่
```
┌ ไอเท็มที่สวมใส่ ──────────────────────────┐
│ [อุปกรณ์ (equipment)] [เงา (Shadow)]      │
│ ┌──────────────┐ ┌──────┐ ┌──────────────┐│
│ │ หมวกผ้า  [รูป]│ │      │ │ผ้าคลุม  [รูป]││
│ └──────────────┘ │ ตัว  │ └──────────────┘│
│  ศีรษะ · head    │ ละคร │  ผ้าคลุม · robe │
│ ┌──────────────┐ │      │ ┌──────────────┐│
│ │+5 ฟัลชั่อน[รูป]│ │      │ │การ์ด   [รูป]││
│ └──────────────┘ └──────┘ └──────────────┘│
│  อาวุธ · R-hand            มือรอง · L-hand │
└───────────────────────────────────────────┘
```

- ทุกช่อง = **ชื่อไอเทมอยู่ซ้าย · รูปไอเทมอยู่ขวาในกรอบสี่เหลี่ยม**
- **ป้ายชื่อช่องเล็ก ๆ อยู่ใต้กล่อง** (ศีรษะ · head / อาวุธ · R-hand / มือรอง · L-hand ...)
  เหมือนหน้าต่างของ RO ไม่ต้องเขียนชื่อช่องซ้ำในกล่อง
- ช่องที่ยังไม่ได้ใส่ของ = กล่องโล่ง ๆ (ดูชื่อช่องจากป้ายใต้กล่อง)
- ซ้าย 4 ช่อง (ศีรษะ · อาวุธ · ชุดเกราะ · ประดับ 1) · ขวา 4 ช่อง (ผ้าคลุม · มือรอง · รองเท้า · ประดับ 2)
- ตัวละครอยู่ตรงกลาง โชว์ท่ายืนของอาวุธที่ถืออยู่
- แท็บบนสุด: **อุปกรณ์ (equipment)** / **เงา (Shadow)** — แท็บเงายังไม่เปิดใช้

> อยากเปลี่ยนชื่อป้ายใต้กล่อง แก้ที่ `SLOT_CAPTION` ในหัวไฟล์ `equipment_window.gd`
> อยากได้กล่องใหญ่/เล็กลง แก้ `SLOT_SIZE` · รูปใหญ่ขึ้น แก้ `ICON_MAX`

### แถมมาด้วย: หน้าต่างไม่บวมค้างอีกแล้ว
เจอบั๊กเก่าตอนทำอันนี้ — **หน้าต่างทุกบานไม่เคยหดกลับ** เพราะไม่ได้อยู่ในคอนเทนเนอร์
(หน้าสวมใส่เคยสูงถึง 2389 px ทั้งที่เนื้อหาแค่ 436 px)

ตอนนี้ `GameWindow.show_window()` เรียก **`fit_to_content()`** ให้อัตโนมัติ
(รอ 1 เฟรมแล้ว `reset_size()`) — สำคัญมากเพราะกรอบหน้าต่างที่ใหญ่เกินจริง
จะไปกินคลิกเมาส์ในที่ที่ไม่มีอะไรอยู่ (ระบบคลิกซ้ายตีของหัวข้อ 7.20)

### ไฟล์ที่แก้
- `scripts/ui/equipment_window.gd` — จัดวางใหม่ทั้งหน้า
- `scripts/ui/game_window.gd` — เพิ่ม `fit_to_content()`

### ข้อควรระวัง
- `btn.get_node("SlotIcon")` **หาไม่เจอ** เพราะรูปอยู่ลึกเข้าไปในกล่องจัดวาง
  ต้องใช้ `btn.find_child("SlotIcon", true, false)`
- ตั้ง `size = get_combined_minimum_size()` ทันทีหลังซ่อน/โชว์หน้าย่อย **จะได้ค่าเก่า**
  ต้อง `await get_tree().process_frame` ก่อนเสมอ แล้วค่อย `reset_size()`

---

## 7.22 ตัวเลขดาเมจใหญ่ขึ้น + ไม่ทับกับ EXP

ข้อความลอยทุกอันตอนนี้ "จองที่" ก่อนโผล่ อันไหนจะทับของเดิมก็ดันขึ้นไปอีกชั้น
แล้วแยกทิศลอยตามชนิด:

| ชนิด | ลอยไปทาง | ขนาด |
|---|---|---|
| ดาเมจที่เราตีมอน | ตรงขึ้น | **34** (คริ **44**) |
| ดาเมจที่เราโดน | ตรงขึ้น | 32 |
| EXP / Job EXP | ขึ้นไปทางขวา | 18 |
| ของที่เก็บได้ / เงิน | ขึ้นไปทางซ้าย | 18 |
| MVP | ตรงขึ้น ค้างนานกว่า | 52 |

- อยากให้ตัวเลขดาเมจใหญ่กว่านี้อีก แก้ `DAMAGE_FONT_SIZE` / `DAMAGE_FONT_CRIT`
  ที่หัวไฟล์ `scripts/entities/monster_base.gd`
- อยากปรับทิศ/ระยะห่าง แก้ `START_OFFSET` และ `DRIFT` ใน `scripts/world/floating_text_layer.gd`

---

## 7.23 ★ ระบบ Base Level กับ Job Level แยกกัน ★

| หลอด | ได้จาก | ขึ้นเลเวลแล้วได้ |
|---|---|---|
| **Base EXP** (เขียว) | ฆ่ามอน / เควส | **แต้มสเตตัส** (Stat Point) + HP/SP เพิ่ม |
| **Job EXP** (ม่วง) | ฆ่ามอน / เควส | **แต้มสกิล** (Skill Point) + ATK/HIT/DEF เล็กน้อย |

- Base สูงสุด 99 · **Job สูงสุด 50** (`MAX_JOB_LEVEL` ใน `player_stats.gd`)
- บนจอโชว์ `Lv.42 / Job 18 นักดาบ` และมีหลอดม่วง JOB เพิ่มมาใต้หลอด EXP
- หน้าสเตตัส (C) โชว์ Base EXP / Job EXP เป็นตัวเลขเต็ม
- Job EXP ที่ได้จากมอน = ช่อง **Job Exp Reward** ใน MonsterData
  **ใส่ 0 = คิดให้เองเป็น 70% ของ EXP ปกติ** (มอนเก่าทุกตัวใช้ได้ทันที ไม่ต้องแก้)
- เควสก็มีช่อง **Reward Job Exp** เหมือนกัน (0 = 70% ของ Reward Exp)
- ไฟล์เซฟเก่ายังโหลดได้ จะเริ่มที่ Job Lv.1

---

## 7.24 ★ บอส King Poring + สกิลระเบิดเหมือก ★

### ต้องวาด Sprite ชื่ออะไรบ้าง
ไฟล์ `data/monsters/king_poring.tres` ช่อง **Sprite Frames** ตอนนี้ชี้ไปที่ `poring_frames.tres`
(ยืมของโพริงมาใช้ก่อนจะได้เล่นได้เลย) — พอวาดเสร็จให้สร้าง SpriteFrames ใหม่แล้วลากมาใส่ช่องนี้แทน

ชื่อท่าใน SpriteFrames:

| ชื่อท่า | ใช้ตอน |
|---|---|
| `Idle` | ยืนเฉย |
| `Run` | เดิน/ไล่ผู้เล่น (ตั้ง `Walk` หรือ `Move` ก็ได้) |
| `Attack` | ตีปกติ |
| **`Skill_SlimeBomb`** | **★ สกิลระเบิดเหมือก ★** |
| `Hit` | โดนตี (ตั้ง `Hurt` ก็ได้) |
| `Die` | ตาย (ตั้ง `Death` หรือ `Dead` ก็ได้) |

- ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ (`skill_slimebomb` = `Skill_SlimeBomb`)
- ไม่มีท่า `Skill_SlimeBomb` ระบบจะถอยไปใช้ท่าชื่อ `Skill` แล้วถอยไป `Attack` ให้เอง
- อยากเปลี่ยนชื่อท่า แก้ช่อง **Skill Anim** ใน king_poring.tres ได้เลย

### ค่าของบอส
Lv.15 · HP 1800 · ATK 34-52 · DEF 12 · EXP 900 / Job 700 · เงิน 400-900z
เกิดใหม่ทุก 120 วินาที · ดรอปฟราคอน, น้ำยาส้ม, เจลโลปี้ และ **การ์ดคิงโพริง 5%**

### สกิลระเบิดเหมือก
ร่ายจากระยะ 380 px · รัศมีโดน 340 x 200 · ดาเมจ x2.4 · คูลดาวน์ 9 วิ · โอกาสร่าย 65%
หลบทันจะขึ้นคำว่า "หลบได้!" · ทุกค่าปรับได้ในกลุ่ม **"สกิลมอนสเตอร์"** ของ .tres

> มอนตัวอื่นก็ใส่สกิลได้เหมือนกัน แค่กรอกช่อง Skill Name + Skill Anim ใน .tres ของตัวนั้น
> ไม่ต้องแก้โค้ดเลย

### ป้าย MVP
บอสตาย = คำว่า **MVP** ตัวใหญ่สีทองเด้งเหนือหัวผู้เล่น + ข้อความแจ้งเตือน
เปลี่ยนคำได้ที่ช่อง **Boss Title** · ทำมอนตัวอื่นเป็นบอสก็แค่ติ๊ก **Is Boss**

---

## 7.25 หน้าตีบวกอุปกรณ์แบบเดียวกับร้านค้า

- ทุกแถวมี **ไอคอนไอเทมในกรอบสี่เหลี่ยม** เหมือนหน้าร้านค้า
- โชว์ระดับตีบวกปัจจุบัน (+0, +5) ไว้ขวาสุด
- กดที่ชื่อ = เลือก + เด้งกล่องรายละเอียดไอเทมข้าง ๆ · แถวที่เลือกอยู่มีกรอบสีทอง
- มุมขวาบนโชว์เงินที่มี

---

## 7.26 แมพใหม่ Asgard Forest 2

- ไฟล์: `scenes/maps/asgard_forest_2.tscn` · id = `asgard_forest_2`
- ประตูเดิมที่คุณทำไว้ชื่อ **ToTown2** (ป้าย "→ Asgard forest 2") ถูกต่อให้แล้ว
  และเพิ่มจุดเกิด **from_forest_2** ในแมพเดิม สำหรับตอนเดินกลับมา
- ในแมพใหม่มี: โพริง · แฟเบร · ลูนาติก กระจายทั่วแมพ + **จุดเกิดบอส King Poring**
  ประตูซ้ายกลับ Forest 1 · ประตูขวาเข้าเมือง
- **พื้นหลังเป็นสีเปล่า ๆ ไว้ก่อน** — วาดเสร็จแล้วเปิดแมพนี้ใน Godot
  เลือกโหนด `Background/Art` แล้วลากไฟล์ภาพใส่ช่อง **Texture** ได้เลย
  (ถ้าภาพใหญ่กว่าพื้นเดิม ปรับ `map_bounds` ของโหนด Map ตามด้วย)

### ไฟล์ที่แก้รอบนี้
`signal_bus` · `player_stats` · `player_state` · `game` · `monster_base` · `player` ·
`dropped_item` · `monster_data` · `quest_data` · `floating_text_layer` ·
`hud` · `ui_theme` · `ui_manager` · `status_window` · `refine_window`
\+ ไฟล์ใหม่ `data/monsters/king_poring.tres` · `data/cards/card_king_poring.tres` ·
`scenes/maps/asgard_forest_2.tscn`
สำรองแมพเดิมไว้ที่ `Sprites/world_node_2d_ก่อนต่อ forest2.tscn.bak`

### บั๊กเก่าที่เจอระหว่างทาง (แก้ให้แล้ว)
**แถบคำใบ้ปุ่มด้านล่างจอหลุดออกนอกจอมาตลอด** และข้อความชื่อแมพไปกองมุมซ้าย
เพราะ `HUD` กับ `UIRoot` ใช้ `set_anchors_preset()` ซึ่ง "รักษากรอบเดิม" ที่ยังกว้าง 0 อยู่
เปลี่ยนเป็น `set_anchors_and_offsets_preset()` แล้ว

---

## 7.27 ต่อสไปรท์ดาบซามู + แก้ของดรอปออร์ค

รันด้วย `python3 _patch_katana.py .` (รันซ้ำได้ ไม่ทำซ้ำซ้อน)

### 1) ออร์คนักรบดรอปของที่ไม่มีอยู่จริง
`orc_warrior.tres` ดรอป `blade` แต่ไอเทมนั้นถูกลบไปแล้ว (ของจริงคือ `novice_sword`)
ของชิ้นนี้เลยไม่เคยหล่นออกมาเลย → แก้เป็น `novice_sword` แล้ว

### 2) ท่าดาบซามู
เพิ่ม 2 ท่าลง `data/sprites/player_frames.tres`:

| ท่า | เฟรม | speed | ภาพ |
|---|---|---|---|
| `Attack_Katana` | 14 | 16 | spritesheet Attack Katana.png |
| `Attack_Katana_bash` | 15 | 13 | spritesheet Attack Katana bash.png |

สคริปต์ **อ่านจากไฟล์ภาพเอง** ว่าช่อง 512x512 ไหนมีรูปอยู่ แล้วเรียงซ้าย→ขวา บน→ล่าง
(วิธีเดียวกับที่ท่า Attack_falchion ทำไว้) — วาดใหม่แล้วลบท่าเดิมทิ้ง รันสคริปต์ซ้ำได้เลย

### 3) ไอเทมดาบซามู
- `attack_animation` → `Attack_Katana` (เดิมชี้ไปที่ `Attack_Blade` เลยใช้ท่าดาบมือใหม่)
- `id` `&"Katana"` → **`&"katana"`** ให้เป็นตัวพิมพ์เล็กเหมือนไอเทมอื่นทุกชิ้น
  (ถ้าปล่อยไว้ เวลาใส่ในรายการดรอป/ร้านค้าด้วยชื่อ `katana` จะหาไม่เจอ)

> **ดาบซามูยังไม่มีที่มาในเกม** — ไม่มีร้านไหนขาย ไม่มีมอนตัวไหนดรอป
> อยากให้ได้มา: เพิ่ม `&"katana"` ลง `shop_items` ของ NPC พ่อค้า หรือใส่ในรายการดรอปของมอน

### ข้อควรระวังตอนแก้ .tres ด้วยสคริปต์
**`sub_resource` ต้องอยู่ก่อนหัวข้อ `[resource]` เสมอ** — ถ้าแทรกไว้หลัง `[resource]`
(เช่นแทรกก่อนบรรทัด `animations = [`) Godot จะอ่านไฟล์ไม่ผ่านทั้งไฟล์
เจอจริงตอนทดสอบรอบนี้ ก่อนแตะไฟล์จริง

### สำรองไว้แล้ว
`data/sprites/player_frames_ก่อนใส่ katana.tres.bak` ·
`data/items/katana_ก่อนแก้.tres.bak` · `data/monsters/orc_warrior_ก่อนแก้.tres.bak`

---

## 7.28 สกิล Slash (พุ่งฟัน) + ผังสกิล + เอฟเฟกต์สกิล

### สกิล Slash
พุ่งตัวไปข้างหน้าเร็ว ๆ **มอนทุกตัวที่ขวางทางโดนดาเมจ** (ตัวละ 1 ครั้งต่อการพุ่ง 1 ครั้ง)
เงื่อนไข: **Bash เลเวล 5 + Base Level 15** · ไฟล์ `data/skills/slash.tres`

ค่าเริ่มต้น: พุ่งไกล 300 px (+15 ต่อเลเวลสกิล) · ความเร็ว 1400 px/วิ ·
ดาเมจ 160% (+30% ต่อเลเวล) · SP 16 · คูลดาวน์ 4 วิ · ชนกำแพงแล้วหยุด

ปรับได้ทั้งหมดในกลุ่ม **"พุ่ง (Dash)"** ของ .tres:
`dash_distance` · `dash_distance_per_level` · `dash_speed` · `dash_range_y` ·
`dash_hit_once` · `dash_stop_on_wall` · และ `max_targets` (0 = ไม่จำกัดจำนวนตัวที่โดน)

**ท่าที่ต้องวาด (ไม่บังคับ):** `Attack_<อาวุธ>_slash` เช่น `Attack_Blade_slash`
ไม่มีก็ถอยไปใช้ `Attack_slash` แล้วถอยไปท่าโจมตีปกติของอาวุธนั้น

> **ทำสกิลพุ่งตัวใหม่:** สร้าง .tres ใหม่ ตั้ง Type = **Active Dash** แล้วกรอกค่าในกลุ่ม "พุ่ง"
> ไม่ต้องแก้โค้ดเลย

### ผังสกิล (Skill Tree)
ระบบเงื่อนไขมีอยู่แล้วใน `SkillData` — รอบนี้ทำให้ **เห็นเป็นผังในหน้าต่างสกิล**:

- สกิลที่ต่อยอดจากสกิลอื่นจะเยื้องเข้าไปพร้อมเส้น `└─` อยู่ใต้สกิลแม่
- แถวที่เงื่อนไขยังไม่ครบ: ตัวหนังสือจาง · ขึ้นคำว่า **( ล็อก )** · ปุ่มเรียนกดไม่ได้ ·
  มุมขวาบอกเงื่อนไขเป็นสีแดง เช่น `Lv.15 · Bash 5`
- กดเลือกสกิลแล้วกล่องล่างบอกเงื่อนไขและเหตุผลที่ยังเรียนไม่ได้

**ตั้งเงื่อนไขสกิลใหม่** — ในไฟล์ .tres ของสกิลนั้น:

| ช่อง | ความหมาย |
|---|---|
| `required_level` | ต้อง Base Level เท่าไหร่ |
| `required_skills` | ต้องมีสกิลอะไรเลเวลเท่าไหร่ เช่น `{"bash": 5}` |

ใส่ได้หลายตัวใน `required_skills` · ตัวแรกในรายการจะถูกใช้จัดผัง (ถือเป็น "สกิลแม่")
**สกิลใหม่ต้องเพิ่ม id ลง `skill_ids` ใน `data/jobs/swordsman.tres` ด้วย** ไม่งั้นจะไม่ขึ้นในหน้าต่าง

---

## 7.29 ★ ทำเอฟเฟกต์สกิลให้ใหญ่/ไกลเกินตัวละคร ★

**ปัญหา:** ถ้าวาดเอฟเฟกต์ลงไปในสไปรท์ตัวละคร (player_frames) เอฟเฟกต์จะโตได้แค่เท่ากรอบภาพตัวละคร
และพุ่งออกไปไกลกว่านั้นไม่ได้

**วิธีแก้:** เอฟเฟกต์เป็น **โหนดแยกที่ไปเกิดในแมพ** ไม่ใช่ลูกของตัวละคร
(ไฟล์ `scripts/entities/skill_effect.gd`) เลยใหญ่แค่ไหน ไกลแค่ไหนก็ได้

### วิธีใช้ — ไม่ต้องเขียนโค้ดเลย
1. วาดเอฟเฟกต์เป็นภาพชุด แล้วสร้าง **SpriteFrames** ใหม่ 1 ไฟล์ (คนละไฟล์กับตัวละคร)
2. เปิด .tres ของสกิล → กลุ่ม **"เอฟเฟกต์สกิล"** → ลากใส่ช่อง **Effect Frames**
3. ปรับตัวเลขให้ได้ตำแหน่ง/ขนาดที่ชอบ

| ช่อง | ความหมาย |
|---|---|
| `effect_frames` | ภาพเอฟเฟกต์ (เว้นว่าง = ไม่มีเอฟเฟกต์) |
| `effect_anim` | ชื่อท่าใน SpriteFrames นั้น (เว้นว่าง = ใช้ท่าแรกที่ไม่ใช่ `default`) |
| `effect_offset` | เยื้องจากตัวละครกี่ px · **x กลับข้างให้เองตามที่ตัวละครหัน** |
| `effect_height` | อยากให้เอฟเฟกต์สูงกี่ px บนจอ (0 = ใช้ `effect_scale`) |
| `effect_speed` | พุ่งออกไปข้างหน้ากี่ px/วินาที (0 = อยู่กับที่) |
| `effect_follow` | เกาะไปกับตัวละคร (ใช้กับสกิลพุ่ง) |
| `effect_life` | อยู่กี่วินาที (0 = จนกว่าอนิเมชันจะเล่นจบ) |
| `effect_delay` | หน่วงกี่วินาทีถึงจะโผล่ |
| `effect_z` | ชั้นการวาด (60 = อยู่หน้าตัวละคร) |

### ตัวอย่างการตั้งค่า
- **ฟันแล้วมีคลื่นพุ่งออกไป:** `effect_offset (110, -60)` · `effect_speed 900` · `effect_life 0.6`
- **เอฟเฟกต์วิ่งไปกับตัวตอนพุ่ง (Slash ใช้แบบนี้):** `effect_follow` เปิด · `effect_life 0.45`
- **ระเบิดรอบตัว:** `effect_offset (0, -40)` · `effect_speed 0` · `effect_height` ใหญ่ ๆ
- **เอฟเฟกต์ใหญ่กว่าตัวละคร 2 เท่า:** ตัวละครสูง 240 px → ตั้ง `effect_height` 400-500 ได้เลย

> ใช้ได้กับสกิลทุกชนิด ไม่ใช่แค่ Slash — ใส่ Effect Frames ให้ bash / magnum_break ก็ได้เหมือนกัน

### ไฟล์ที่แก้รอบนี้
`skill_data.gd` (เพิ่ม type **Active Dash** + กลุ่มพุ่ง + กลุ่มเอฟเฟกต์) ·
`player.gd` (การพุ่ง + เรียกเอฟเฟกต์) · **ไฟล์ใหม่** `skill_effect.gd` ·
`skill_window.gd` (ผังสกิล) · **ไฟล์ใหม่** `data/skills/slash.tres` ·
`swordsman.tres` (เพิ่ม slash ลงสายอาชีพ · สำรองไว้ที่ `swordsman_ก่อนเพิ่ม slash.tres.bak`)

### ข้อควรระวัง
- **enum `SkillType` ต้องเพิ่มต่อท้ายเท่านั้น** (Active Dash = 5) เพราะไฟล์ .tres
  เก็บค่า type เป็น "ตัวเลข" — สลับลำดับเมื่อไหร่ สกิลเก่ากลายเป็นคนละชนิดทันที
- อีโมจิ (🔒) ในหน้าต่างเกมขึ้นเป็นสี่เหลี่ยม เพราะฟอนต์ไทยไม่มีตัวอีโมจิ — ใช้ข้อความธรรมดาแทน

---

## 7.30 ★ ปรับขนาดมอนสเตอร์เอง (ทำเองได้ทุกตัว) ★

เปิด `data/monsters/<ชื่อมอน>.tres` ใน Godot แล้วดูที่ Inspector — มี **4 ช่อง** ที่เกี่ยวกับขนาด

| ช่อง | ความหมาย | ควรตั้งเท่าไหร่ |
|---|---|---|
| **Display Height** | อยากให้ตัวสูงกี่พิกเซลบนจอ | **ปรับตัวนี้ตัวเดียวก็พอ** |
| Hitbox Size | กล่องชน (กว้าง, สูง) | ประมาณ 0.5 เท่าของ Display Height |
| Hp Bar Offset Y | หลอดเลือดลอยสูงจากจุดกึ่งกลางเท่าไหร่ | ติดลบ · ประมาณ −0.8 เท่าของความสูง |
| Attack Range | ระยะที่มอนตีถึง | ตัวใหญ่ควรมากกว่าตัวเล็ก |

**ทำไมแค่ Display Height ก็พอ:** ระบบวัดขนาดจริงของภาพ (ไม่นับพื้นที่โปร่งใส)
แล้วย่อ/ขยายให้สูงเท่าที่ตั้งไว้เสมอ — ไฟล์ภาพจะใหญ่แค่ไหนก็ได้ ไม่ต้องมานั่งคำนวณ Scale เอง
และจัดเท้าให้แตะพื้นให้อัตโนมัติด้วย (ช่อง Align Feet)

**ตัวอย่าง** ตอนนี้ King Poring ตั้งไว้:
```
Display Height  = 300     (โพริงธรรมดา 85 → ใหญ่กว่า 3.5 เท่า)
Hitbox Size     = 170 x 160
Hp Bar Offset Y = -250
Attack Range    = 150
```
อยากให้ใหญ่กว่านี้อีก เช่น 400 px ก็แก้ Display Height เป็น 400 แล้วขยับอีก 3 ช่องตามสัดส่วน
(หรือรัน `python3 _patch_bosssize.py .` หลังแก้ตัวเลขในไฟล์ `_patch_bosssize.py` ก็ได้)

> **ตัวละครผู้เล่นใช้กติกาเดียวกัน** — ช่อง **Auto Fit Height** ในโหนด Player (ตอนนี้ 240)

---

## 7.31 เอฟเฟกต์สกิลของมอนสเตอร์และบอส

กติกาเดียวกับเอฟเฟกต์สกิลของผู้เล่น (หัวข้อ 7.29) แต่ตั้งใน **MonsterData** กลุ่ม **"เอฟเฟกต์สกิลมอน"**

| ช่อง | ความหมาย |
|---|---|
| `skill_effect_frames` | SpriteFrames ของเอฟเฟกต์ (เว้นว่าง = ไม่มีเอฟเฟกต์) |
| `skill_effect_anim` | ชื่อท่าใน SpriteFrames นั้น |
| `skill_effect_offset` | เยื้องจากตัวมอนกี่ px (x กลับข้างตามที่มอนหันให้เอง) |
| `skill_effect_height` | อยากให้เอฟเฟกต์สูงกี่ px บนจอ |
| `skill_effect_speed` | พุ่งออกไปข้างหน้ากี่ px/วิ |
| `skill_effect_follow` | เกาะไปกับตัวมอน |
| `skill_effect_life` | อยู่กี่วินาที (0 = ตามความยาวอนิเมชัน) |
| `skill_effect_delay` | หน่วงกี่วิถึงโผล่ — **ตั้งเท่า Skill Windup จะระเบิดพร้อมดาเมจพอดี** |

**ตัวอย่างสำหรับ "ระเบิดเหมือก" ของ King Poring:**
`skill_effect_height 380` · `skill_effect_offset (0, -60)` · `skill_effect_speed 0` ·
`skill_effect_follow` ปิด · `skill_effect_delay 0.75` (= Skill Windup)

ตอนนี้ครบทั้ง 3 ฝั่งแล้ว: **ผู้เล่น · มอนสเตอร์ · บอส** ใช้โหนดเอฟเฟกต์ตัวเดียวกัน
(`scripts/entities/skill_effect.gd`) เลยใหญ่/ไกลเกินตัวได้เหมือนกันหมด

---

## 7.32 ผังสกิลแบบใหม่ (หน้าต่างสกิล กด K)

หน้าตาเป็นตารางแบบเกมจริงแล้ว — หนึ่งช่องประกอบด้วย

```
   [ 5/10 ]     <- ป้ายเลเวล
   ( ไอคอน )
   [  Bash  ]   <- ป้ายชื่อ
   [เรียน +1]
```

- สกิลลูกอยู่ **แถวถัดลงไป** พร้อม **เส้นโยง** จากสกิลแม่ลงมา
- สกิลแม่จะอยู่ **กึ่งกลางเหนือกลุ่มลูก** ของตัวเอง
- ช่องที่เงื่อนไขยังไม่ครบ = **สีเทา + เครื่องหมาย !** และปุ่มเรียนกดไม่ได้
- กดที่ไอคอนเพื่อเลือก แล้วดูรายละเอียด/เงื่อนไขในกล่องด้านล่าง

ปรับหน้าตาได้ที่หัวไฟล์ `scripts/ui/skill_window.gd`:
`TILE` (ขนาดไอคอน) · `COL_W` (ความกว้างคอลัมน์) · `GAP_X` / `GAP_Y` (ระยะห่าง)

---

## 7.33 ★ แถบปุ่มลัดใหม่ + ช่องยา HP/SP ★

### หน้าตา
แถบปุ่มลัด **แยกออกมาเป็นแผงของตัวเอง** อยู่ใต้แผงหลอดเลือด ช่องเป็น **สี่เหลี่ยมจัตุรัส 58x58**

```
[1][2][3][4]   [Q ยาเลือด][R ยามานา]   [Tab เมนู]
```

- ช่องยาโชว์ **รูปยาที่ตั้งไว้** + **จำนวนที่เหลือ** มุมขวาล่าง (เหลือ 0 = เลขเป็นสีแดง)
- ปุ่ม **Q** = ยาเลือด · ปุ่ม **R** = ยามานา (ปุ่ม R เพิ่มใหม่ ระบบสร้างให้ตอนเปิดเกม ไม่ต้องรีสตาร์ท)

### เลือกยาเองได้
เปิดกระเป๋า (I) → **กดที่ยา** → แถว "ตั้งเป็นยาด่วน" จะโผล่ขึ้นมา → กด **ช่อง Q** หรือ **ช่อง R**
ยาที่ตั้งอยู่แล้วจะมีดาว ★ นำหน้าปุ่ม · ยาชิ้นเดียวกันอยู่ได้ช่องเดียว · **เซฟไปกับไฟล์เซฟด้วย**
(เลือกได้เฉพาะของประเภทของกิน/ยา — ของสวมใส่แถวนี้จะไม่โผล่)

### ปรับเอง
`scripts/ui/hud.gd` หัวไฟล์:
`HOTKEY_SIZE` (ขนาดช่อง) · `HOTKEY_PANEL_POS` (ตำแหน่งแผง — ค่าเริ่มต้น 12, 150)

### ข้อควรระวังที่เจอตอนทำ
**ปุ่มของ Godot กันที่ให้ข้อความ 1 บรรทัดเสมอ** ต่อให้ text ว่างเปล่า —
ช่องเลยสูงเกินจนไม่เป็นจัตุรัส (58 กลายเป็น 62) แก้โดยตั้ง `font_size` ของปุ่มเป็น **1**
แล้วย้ายข้อความทั้งหมดไปไว้ที่ "ป้ายซ้อน" (Label ลูกของปุ่ม) แทน

### ไฟล์ที่แก้รอบนี้
`player_state` (ช่องยาด่วน + เซฟ) · `input_setup` (ปุ่ม R) · `player` (กด Q/R) ·
`skill_effect` (รองรับมอน) · `monster_base` · `monster_data` (ช่องเอฟเฟกต์) ·
`hud` (แถบปุ่มลัดใหม่) · `ui_manager` · `inventory_window` (ตั้งช่องยา) · `skill_window` (ผังกริด)
สำรอง: `data/monsters/king_poring_ก่อนขยาย.tres.bak`

---

## 7.34 แถบปุ่มลัด — ย้ายไปข้างขวาหลอดเลือด และลากย้ายได้ (รอบ 21)

### ตำแหน่งเริ่มต้น
แถบปุ่มลัดไม่อยู่ข้างล่างแผงหลอดเลือดแล้ว ย้ายไป **ข้างขวา แถวเดียวกัน**
เป็นคนละแผงกัน มีช่องไฟคั่นไว้ ไม่ได้เชื่อมติดกัน

ระยะห่างแก้ได้ที่ `scripts/ui/hud.gd`:

```gdscript
const HOTKEY_GAP_X := 10.0        # ห่างจากแผงหลอดเลือดกี่พิกเซล
const HOTKEY_SIZE := Vector2(58, 58)   # ขนาดช่อง (จัตุรัส)
```

ระบบคำนวณตำแหน่งเองจากความกว้างจริงของแผงหลอดเลือด (`default_hotkey_pos()`)
ถ้าเพิ่มหลอดใหม่จนแผงกว้างขึ้น แถบปุ่มลัดจะขยับตามให้เอง

### ลากย้ายได้
- **ลากพื้นที่ว่างบนแผง** หรือ **ลากที่จุด `[::]` ด้านซ้ายของแถบ** = ย้ายไปวางตรงไหนก็ได้
- ปุ่มในแถบยังกดได้ตามปกติ (ลากโดนแต่พื้นแผง ไม่โดนปุ่ม)
- ลากออกนอกจอไม่ได้ ระบบดึงกลับให้อยู่ในจอเสมอ (`_clamp_hotkey_panel()`)
- **คลิกขวาบนแผง = คืนตำแหน่งเดิม** (กลับไปอยู่ข้างขวาแผงหลอดเลือด)

### ตำแหน่งถูกจำไว้
เก็บที่ `user://ui_layout.cfg` (คนละไฟล์กับไฟล์เซฟ ใช้ร่วมกันทุกช่องเซฟ)

```
[hud]
hotkey_pos = Vector2(318, 12)
```

บน Windows ไฟล์นี้อยู่ที่ `%APPDATA%\Godot\app_userdata\<ชื่อโปรเจกต์>\ui_layout.cfg`
ถ้าอยากรีเซ็ตตำแหน่ง ลบไฟล์นี้ทิ้ง หรือคลิกขวาบนแถบก็ได้

### เบื้องหลัง (ถ้าจะทำแผงอื่นให้ลากได้บ้าง)
1. แผง (`PanelContainer`) ต้องเป็น `MOUSE_FILTER_STOP` — ถ้าเป็น PASS จะลากไม่ติด
2. แถวข้างใน (`HBoxContainer`) ต้องเป็น `MOUSE_FILTER_IGNORE` — คลิกจะได้ทะลุไปโดนแผง
   **ปุ่มที่เป็นลูกยังกดได้ตามปกติ** (IGNORE ที่พ่อไม่ได้บังลูก)
3. จับ `gui_input` ของแผงเพื่อ "เริ่มลาก" แล้วจับ `_input()` ที่ระดับ HUD เพื่อ "ลากต่อ"
   — เพราะลากเร็ว ๆ เมาส์จะหลุดออกนอกแผง แล้ว `gui_input` จะหยุดส่งเหตุการณ์
4. `UI.is_point_over_ui()` ต้องรู้จักแผงนี้ด้วย ไม่งั้นคลิกบนแถบแล้วตัวละครจะตี

### กับดักที่เจอ
- **ห้ามตั้ง `.text` ให้ปุ่มช่องลัดเด็ดขาด** — Godot จะกันที่ให้ข้อความ 1 บรรทัดเสมอ
  ช่องจะสูงเกินจนไม่เป็นจัตุรัส ข้อความทั้งหมดอยู่ที่ป้ายซ้อน `SlotName` / `SlotCount` แล้ว
  (โค้ดเก่าใน `_process()` ที่เขียน `potion_button.text` ถูกลบออกแล้ว)

---

## 7.35 ระยะการฟัน — วัดจาก "ขอบตัวมอน" ไม่ใช่กลางตัว (รอบ 22)

### ปัญหาเดิม
ระบบเก่าวัด **จุดกึ่งกลางถึงจุดกึ่งกลาง** เลยเพี้ยน 3 แบบ

1. **บอสตัวใหญ่ต้องเดินไปประชิดมาก** — กึ่งกลางบอสอยู่ลึกเข้าไป 85 px จากขอบตัว
   ยืนติดตัวบอสแล้วยังตีไม่ถึงกึ่งกลาง
2. **มอนที่ยืนทับ/เราเหยียบอยู่ไม่โดนเลย** — อยู่ข้างหลังนิดเดียวก็ถูกตัดทิ้งทั้งตัว
   และตัวที่อยู่ใต้เท้าก็หลุดเงื่อนไขแนวตั้ง
3. **โพริงที่กำลังกระโดดไม่โดน** — ปลายเท้าลอยขึ้นเกินระยะแนวตั้ง

### ระบบใหม่: กรอบชนกรอบ
- **กรอบดาบ** (`player.attack_rect()`) เป็นสี่เหลี่ยมรอบตัวเรา
- **กรอบตัวมอน** (`monster.body_rect()`) เป็นสี่เหลี่ยมเท่าขนาดตัวจริงบนจอ
- สองกรอบแตะกันเมื่อไหร่ = โดน (ขอบชนขอบก็นับ)

### ค่าที่ปรับได้ (เลือก Player ใน Scene แล้วดูใน Inspector)

| ช่อง | ค่าใหม่ | ความหมาย |
|---|---|---|
| `Attack Range X` | **150** (เดิม 110) | ดาบยื่นไปข้างหน้ากี่พิกเซล วัดถึง **ขอบ** ตัวมอน |
| `Attack Range Y` | **200** (เดิม 70) | เอื้อมขึ้นเหนือปลายเท้าเท่าไหร่ (ครอบทั้งตัวขึ้นไปบนหัว) |
| `Attack Back Reach` | **55** | เอื้อมไปข้างหลังเท่าไหร่ — ตัวที่เบียด/ทับเราอยู่จะได้โดน |
| `Attack Reach Down` | **40** | ฟันต่ำกว่าปลายเท้าลงไปเท่าไหร่ — ตัวที่เราเหยียบอยู่จะได้โดน |

**ระยะจริงที่ได้ = 150 + ครึ่งความกว้างตัวมอน**
โพริง (กว้าง 50) → 175 px · King Poring (กว้าง 170) → **235 px**

### กรอบตัวมอนคิดมาจากอะไร (`monster_base.body_size()`)
- ความสูง = `Display Height` (ถ้าไม่ได้ตั้ง จะวัดจากสไปรท์จริง)
- ความกว้าง = `Hitbox Size.x` แต่ไม่น้อยกว่า 35% ของความสูง (กันตัวสูงแต่กล่องแคบจนฟันยาก)
- **ขยายมอนให้ใหญ่ขึ้น = กรอบนี้ใหญ่ตามเอง ไม่ต้องไปแก้ระยะที่ไหนอีก**

### ระยะตีของมอนก็แก้ตามด้วย
`monster_base.attack_reach()` = `Attack Range` ที่ตั้งใน .tres **+ ครึ่งความกว้างตัวมัน**
บอสจึงตีถึงจากขอบตัวมัน ไม่ต้องเอากึ่งกลางตัวมาจ่อผู้เล่น
(King Poring: ตั้งไว้ 150 → ใช้จริง 235)

### สกิลใช้กรอบเดียวกันหมด
`range_x` / `range_y` ใน SkillData ความหมายเปลี่ยนตามด้วย —
`range_x` = ยื่นไปข้างหน้าถึง **ขอบ** ตัวมอน · `range_y` = เอื้อมขึ้นเหนือปลายเท้า
สกิลรอบตัว (Magnum Break) และสกิลพุ่ง (Slash) ใช้กรอบสมมาตรซ้าย-ขวา

### อยากดูกรอบเองตอนเล่น
ใส่โค้ดนี้ชั่วคราวใน `player.gd`:

```gdscript
func _draw() -> void:
    var r := attack_rect(attack_range_x, attack_range_y)
    draw_rect(Rect2(r.position - global_position, r.size), Color.RED, false, 3.0)

func _process(_d): queue_redraw()
```

---

## 7.36 ผังสกิลโฉมใหม่ — ปุ่ม + บนรูป · กล่องรายละเอียดเด้งข้าง ๆ (รอบ 23)

### เปลี่ยนอะไรบ้าง
| เดิม | ใหม่ |
|---|---|
| ปุ่ม **"เรียน +1"** เป็นแถบใต้ทุกช่อง (มีตลอด กดไม่ได้ก็ยังอยู่) | **ปุ่มกลม `+` สีเขียวที่มุมขวาล่างของรูป** โผล่เฉพาะตอนอัพได้จริง |
| แถบข้อความรายละเอียดอัดอยู่ท้ายหน้าต่าง | **กดที่รูป → กล่องเด้งข้าง ๆ หน้าต่าง พร้อมไอคอนใหญ่** (แบบเดียวกับดูไอเทม) |
| แถวปุ่ม "ตั้งปุ่มลัด 1-4" ค้างอยู่ท้ายหน้าต่าง | ย้ายไปอยู่ในกล่องรายละเอียด (โผล่เฉพาะสกิลที่เรียนแล้ว) |

**หลักคิด:** ในผังเหลือแค่ "ดู" กับ "กด +" · ส่วน "อ่านรายละเอียด / ตั้งช่องลัด"
ไปอยู่ในกล่องที่เปิดตอนเลือกสกิล — หน้าต่างเลยโล่งและเข้าใจง่ายกว่าเดิม

### ปุ่ม + (`skill_window.gd`)
- ขนาดแก้ที่ `const PLUS_SIZE := 24.0`
- **โผล่เฉพาะตอน `book.can_learn()` เป็นจริง** — เงื่อนไขครบ + มีแต้ม + ยังไม่เต็มเลเวล
  เห็นปุ่มเมื่อไหร่ = กดได้แน่นอน ไม่ต้องเดา
- tooltip บอกชัด: "ใช้ 1 แต้มสกิล อัพ Bash เป็นเลเวล 6"
- ★ ปุ่มต้องอยู่ **ในกรอบ** ของปุ่มไอคอน ★ เพราะปุ่มไอคอนตั้ง `clip_contents = true`
  ล้นออกไปแม้แต่นิดเดียวก็โดนตัดหายทั้งปุ่ม (เจอมาแล้ว)

### กล่องรายละเอียด
ใช้ `ItemInfoPopup` ตัวเดียวกับกล่องไอเทม เพิ่มความสามารถใหม่ 2 อย่าง:

```gdscript
UI.show_info(title, icon, body_bbcode, anchor, color, actions)
```
`actions` เป็นรายการ Dictionary:

| รูปแบบ | ความหมาย |
|---|---|
| `{"text": "...", "on": Callable, "disabled": bool}` | ปุ่มเต็มความกว้าง |
| `{"row": [ {...}, {...} ]}` | หลายปุ่มในแถวเดียว (ใช้กับช่องลัด 1-4) |
| `{"hint": "ข้อความ", "hint_color": Color}` | บรรทัดคำอธิบาย ไม่ใช่ปุ่ม |

ในกล่องสกิลจะมี: ประเภท · เลเวล x/y · เงื่อนไข (เขียว=ครบ แดง=ไม่ครบ) ·
ค่าดาเมจ/ฟื้นฟู/บัฟตามเลเวลปัจจุบัน · SP + คูลดาวน์ · คำอธิบาย ·
ปุ่ม "อัพเป็นเลเวล N (ใช้ 1 แต้ม)" · ปุ่มช่องลัด 1-4 (ช่องที่ตั้งไว้แล้วมี `*`)

### ขนาดและตำแหน่งหน้าต่าง
- หน้าต่างผังสกิลย้ายไปที่ **(60, 110)** (เดิม 240, 130) และกว้าง 700
  **เหตุผล:** กล่องรายละเอียดกว้าง 380 ต้องมีที่ยืนข้างขวา ไม่งั้นมันจะเด้งไปทับผังเอง
  ถ้าจะขยายหน้าต่างผังอีก ต้องเช็คว่า `x + กว้าง + 8 + 380 <= 1276` ไม่งั้นกล่องจะทับ
- ความกว้างผัง = `max_col * (COL_W + GAP_X) + COL_W` — **ห้ามบวกช่องไฟของช่องสุดท้าย**
  ไม่งั้นกว้างเกินหน้าต่างนิดเดียว แล้วมีแถบเลื่อนแนวนอนโผล่มาโดยไม่จำเป็น
- ชื่อสกิลยาวเกิน 11 ตัวจะถูกตัด (`_short()`) เช่น "Magnum Break" → "Magnum Brea."

### เทสต์
`hud2_test` (91 เคส) ครอบคลุมทั้งหมด — ปุ่ม + อยู่บนรูป · ช่องที่ล็อกไม่มีปุ่ม + ·
กดสกิลแล้วกล่องเปิด · กล่องมีไอคอน/เลเวล/ปุ่มอัพ/ปุ่มช่องลัด · กดปุ่มในกล่องแล้วเลเวลขึ้นจริง

**กับดักที่เจอตอนเขียนเทสต์:** lambda ใน GDScript จับตัวแปรนอกแบบ "สำเนา" —
เขียน `var found = null` แล้วให้ lambda ไปตั้งค่า จะไม่ได้ค่าออกมา ต้องใส่ผลลง `Array` แทน

---

## 7.37 กล่องสนทนา — คุยกับตัวละครแบบมีรูปครึ่งตัว (รอบ 24)

เควส/บทคุยไม่ใช้ "กล่องยืนยันกลางจอ" แล้ว เปลี่ยนเป็น **กล่องสนทนาล่างจอ + รูปตัวละครยืนข้าง ๆ**
เพื่อให้ใส่เนื้อเรื่องยาว ๆ ได้ลื่น (ไฟล์ `scripts/ui/dialogue_box.gd`)

### หน้าตา
- กล่องข้อความอยู่ล่างจอ · **รูปตัวละครยืนติดขอบล่าง ล้ำขึ้นไปเหนือกล่อง** · กล่องหลบให้รูปเอง
- ป้ายชื่อผู้พูดลอยอยู่มุมบนของกล่อง (คนพูดอยู่ฝั่งขวา ป้ายจะย้ายไปขวา)
- ข้อความ **ขึ้นทีละตัวอักษร** — กด F / Enter / คลิก 1 ครั้ง = ขึ้นครบทันที · กดอีกที = บรรทัดถัดไป
- **คนที่ไม่ได้พูดจะหรี่ลง** เหมือนเกม RPG ทั่วไป
- แถบคำใบ้ปุ่มล่างจอถูกซ่อนอัตโนมัติระหว่างคุย

### เขียนบทสนทนา

```gdscript
var pick: int = await UI.talk([
    {"name": "ช่างตีเหล็กฮันส์", "portrait": tex, "side": 0, "text": "ว่าไงเจ้าหนู"},
    {"name": "คุณ",             "side": 1,       "text": "มีงานให้ทำมั้ย"},
    {"name": "ช่างตีเหล็กฮันส์", "side": 0, "text": "มีสิ",
     "info": "เงื่อนไข: ล่าโพริง 100 ตัว\nรางวัล: ...",
     "choices": ["รับเควส", "ไว้ก่อน"]},
])
# pick = ลำดับปุ่มที่กด (0 = อันแรก) · -1 = บทนี้ไม่มีตัวเลือก · Esc = อันสุดท้าย
```

| คีย์ | ความหมาย |
|---|---|
| `name` | ชื่อผู้พูด (ไม่ใส่ = ไม่โชว์ป้ายชื่อ) |
| `text` | ข้อความ (ใส่ BBCode ได้ เช่น `[color=#ffe14a]...[/color]`) |
| `portrait` | `Texture2D` หรือ path `"res://..."` — ไม่ใส่ = ใช้รูปเดิมของฝั่งนั้น |
| `side` | 0 = รูปซ้าย · 1 = รูปขวา |
| `info` | ข้อความเสริมตัวเล็กสีทอง (เงื่อนไข/รางวัล) |
| `choices` | รายชื่อปุ่ม — มีเมื่อไหร่จะรอให้เลือกก่อนไปต่อ |

คุยประโยคเดียวจบ: `await UI.say_as("ชื่อ", "ข้อความ", รูป, ฝั่ง)`

### ใส่รูปให้ NPC
1. วาดรูป **ครึ่งท่อนบน (หัวถึงเอว) พื้นหลังโปร่งใส สูง 400-500 px**
2. เซฟไว้ที่ `Sprites/portraits/<ชื่อ>.png`
3. ใน Godot เลือก NPC คนนั้น → กลุ่ม **"รูปตัวละครในกล่องสนทนา"**
   - `Portrait` — ลากไฟล์ภาพมาใส่ (ทางที่ง่ายสุด)
   - `Portrait File` — หรือพิมพ์ path เช่น `res://Sprites/portraits/tony.png`
   - `Portrait Side` — ซ้าย / ขวา
4. ไม่ใส่รูปก็คุยได้ปกติ กล่องจะกินเต็มความกว้างแทน

**ทำตัวอย่างไว้แล้ว:** ช่างตีเหล็กฮันส์ (`Sprites/portraits/hans.png`)
— ตัดมาจากภาพเต็มตัวที่ส่งมา เอาเฉพาะหัวถึงเอว แล้วย่อให้สูง 420 px

### ตัดรูปเองด้วย python (ถ้ามีภาพเต็มตัว)
```python
from PIL import Image
im = Image.open("ภาพเต็มตัว.png").convert("RGBA")
im = im.crop(im.getbbox())                    # ตัดขอบโปร่งใสทิ้ง
w, h = im.size
bust = im.crop((0, 0, w, int(h * 0.56)))      # เอาแค่ 56% บน = หัวถึงเอว
bust = bust.crop(bust.getbbox())
k = 420 / bust.size[1]
bust.resize((int(bust.size[0] * k), 420)).save("Sprites/portraits/ชื่อ.png")
```

### NPC พูดเองอัตโนมัติ
- `npc.line("ข้อความ", "ข้อมูลเสริม", ["ปุ่ม1","ปุ่ม2"])` = สร้างบทพูด 1 บรรทัดพร้อมชื่อ+รูปของ NPC คนนั้น
- NPC แบบ **DIALOG** (ไกด์): ช่อง `Dialog` **เว้นบรรทัดว่าง 1 บรรทัด = ขึ้นหน้าใหม่** ในกล่อง
- เควส: ชวนรับ / บอกความคืบหน้า / ส่งเควส ใช้กล่องนี้หมดแล้ว

### ปรับขนาด
`dialogue_box.gd`: `BOX_HEIGHT 150` (ส่วนข้อความ) · `INFO_H 44` · `CHOICE_H 44` ·
`PORTRAIT_HEIGHT 330` (รูปสูงบนจอ) · `PORTRAIT_SLOT 300` (กล่องหลบให้กี่ px) ·
`TEXT_SPEED 55` (ตัวอักษรต่อวินาที) · `BOX_MARGIN 28`

### กับดักที่เจอ
- **ป้ายที่ตัดบรรทัดเอง (autowrap) ตอนความกว้างยังเป็น 0 จะคิดความสูงขั้นต่ำเป็นพันพิกเซล**
  (เพราะมันตัดบรรทัดทีละตัวอักษร) แล้วดันกล่องยาวทะลุจอ —
  แก้ด้วยการตั้ง `custom_minimum_size.x` ให้ป้ายก่อน แล้วจัดกล่องซ้ำอีกรอบหลังผ่านไป 1 เฟรม
- **กล่องที่สูงเปลี่ยนไปตามเนื้อหา ต้องยึด "ขอบล่าง"** ไม่งั้นพอมีปุ่มตัวเลือกกล่องจะงอกลงนอกจอ

### หน้าตากล่อง (ปรับรอบ 25)
กล่องเปลี่ยนเป็น **พื้นขาวนวล ตัวหนังสือเข้ม** ไม่มีขอบทองแล้ว อ่านง่ายขึ้นเยอะ

| ค่าคงที่ใน `dialogue_box.gd` | ค่า | ความหมาย |
|---|---|---|
| `BOX_BG` | `#f6f3ebfa` | พื้นกล่อง (ขาวนวล) |
| `BOX_EDGE` | `#2b3346` | เส้นขอบบาง 2 px มุมมน 14 + เงาใต้กล่อง |
| `NAME_BG` | `#232c42fa` | ป้ายชื่อ พื้นเข้ม ตัวหนังสือทอง ตัดกับกล่องขาว |
| `TEXT_DARK` | `#1c2231` | ตัวหนังสือหลัก |
| `INFO_DARK` | `#8a5a12` | ข้อความเสริม (เงื่อนไข/รางวัล) |
| `FONT_TEXT` | **22** | ขนาดตัวหนังสือหลัก (เดิม 17) |
| `FONT_NAME` / `FONT_INFO` / `FONT_HINT` | 19 / 15 / 13 | ป้ายชื่อ / ข้อความเสริม / คำใบ้ |
| `PAD_X` `PAD_TOP` `PAD_BOTTOM` | 22 / 16 / 12 | ขอบในกล่อง (ใช้ `MarginContainer` ชื่อ `Pad`) |

**ที่แก้เพิ่ม**
- **ป้ายชื่อวางเหนือกล่องทั้งป้าย** (เดิมคร่อมขอบกล่องอยู่ครึ่งหนึ่ง เลยบังบรรทัดแรก)
- **กล่องไม่ทับรูปตัวละครแล้ว** — กล่องทึบ ทับเมื่อไหร่ตัวละครโดนบังครึ่งตัว
  (`box_left = BOX_MARGIN + PORTRAIT_SLOT` ไม่ลบ 40 เหมือนเดิม)
- **รูปตัวละครวาดทีหลังกล่อง** (`move_child`) ตัวละครจะได้ยืน "หน้า" กล่องเสมอ
- มีเส้นคั่นบาง ๆ เหนือข้อความเสริม · ปุ่มตัวเลือกใหญ่ขึ้น (สูง 38 ฟอนต์ 16)
- ฉากหลังมืดลงนิด (0.42) ให้กล่องขาวเด่นขึ้น

---

## 7.38 ปุ่มจอสัมผัส (มือถือ) + เอาขึ้นเว็บ (รอบ 26)

### ปุ่มจอสัมผัส — `scripts/ui/touch_controls.gd`

**ทำไมต้องเขียนเอง ไม่ใช้ Button ธรรมดา**
Godot แปลง "นิ้วแรก" เป็นเมาส์ให้เท่านั้น ถ้าใช้ Button ปกติจะกดได้ทีละปุ่ม
เดินไปตีไปไม่ได้ ไฟล์นี้เลยอ่าน `InputEventScreenTouch` เองแยกตามหมายเลขนิ้ว (multi-touch)

**ปุ่มส่งออกเป็น "action" ทั้งหมด** เกมส่วนอื่นเลยไม่ต้องแก้อะไรเลย
ส่ง 2 ทางพร้อมกันเพื่อให้ครบทุกแบบที่โค้ดเช็ค:
- `Input.action_press(action)` — สำหรับโค้ดที่เช็คแบบ polling (`Input.is_action_pressed`)
- `Input.parse_input_event(InputEventAction)` — สำหรับโค้ดที่เช็คใน `_input`/`_unhandled_input`
  (เช่น NPC กด F คุย · กล่องสนทนากด F ไปต่อ) — **สองอย่างนี้ขาดอันใดอันหนึ่งไม่ได้**

**ปุ่มที่มี** (13 ปุ่ม)
- ซ้ายล่าง: ◀ ▶ (เดิน) · ▼ (ลงจากแท่น)
- ขวาล่าง: โจมตี · กระโดด · คุย/เก็บ
- แถวถัดขึ้นไป: สกิล 1 2 3 4
- แถวบนสุด: ยา · มานา · เมนู

**ขนาด/ตำแหน่ง** แก้ได้ที่ค่าคงที่ด้านบนไฟล์:
`PAD_BIG 112` (เดิน/โจมตี) · `PAD_MID 96` (กระโดด/คุย) · `PAD_SMALL 78` (สกิล) ·
`PAD_TINY 68` (ยา/เมนู) · `EDGE 22` (ห่างขอบจอ) · `GAP 10` (ห่างกัน) ·
`IDLE_ALPHA 0.55` / `HOLD_ALPHA 1.0` (ความจางตอนไม่กด/กด)

**โหมด** `UI.touch.set_mode(TouchControls.Mode.AUTO / ON / OFF)`
- **AUTO** = โผล่เองเมื่อ `DisplayServer.is_touchscreen_available()` หรือเมื่อมีการแตะจอครั้งแรก
- เปลี่ยนได้ในเกมที่ **เมนูระบบ (Tab) → ปุ่มจอสัมผัส** (กดวนไป อัตโนมัติ → เปิดตลอด → ปิด)
- จำไว้ที่ `user://ui_layout.cfg` คีย์ `[touch] mode`

**พฤติกรรมอื่น**
- เปิดหน้าต่าง / คุยกับ NPC อยู่ = ซ่อนปุ่มให้อัตโนมัติ (กันบังปุ่มในหน้าต่าง)
- ซ่อนแถบคำใบ้ปุ่มคีย์บอร์ดล่างจอตอนเปิดปุ่มจอสัมผัส
- ลากนิ้วจากปุ่มหนึ่งไปอีกปุ่ม = สลับปุ่มให้เลย (สไลด์ ◀ ↔ ▶ ได้ลื่น)
- ปิดโหมดตอนกดค้างอยู่ = ปล่อย action ให้ครบ ไม่มีค้าง
- `UI.is_point_over_ui()` นับปุ่มพวกนี้ด้วย → แตะปุ่มแล้วตัวละครไม่ฟันมั่ว

### เอาขึ้นเว็บ / GitHub
ดูขั้นตอนละเอียดที่ **`คู่มืออัพขึ้น GitHub.md`** (ไฟล์แยก)

ไฟล์ที่เตรียมไว้แล้ว:
| ไฟล์ | หน้าที่ |
|---|---|
| `export_presets.cfg` | preset ชื่อ **`Web`** · **`variant/thread_support=false`** (สำคัญ! GitHub Pages ตั้ง COOP/COEP header ไม่ได้) · ออกที่ `build/web/index.html` |
| `.github/workflows/deploy.yml` | GitHub Actions: โหลด Godot ตาม `GODOT_VERSION` → `--import` 2 รอบ → `--export-release "Web"` → ขึ้น GitHub Pages |
| `.gitignore` | ตัด `.godot/` · `build/` · `_update*.zip` · `*.bak` |
| `README.md` | หน้าแรกของ repo |

**จุดที่พลาดง่าย**
- `--headless --import` **ต้องรัน 2 รอบ** รอบแรก error เรื่องลำดับ `class_name` ได้
- ชื่อ preset ต้องเป็น `Web` เป๊ะ ๆ ตรงกับคำสั่ง `--export-release "Web"`
- โปรเจกต์ตั้ง `stretch/mode=canvas_items` + `aspect=expand` อยู่แล้ว → จอมือถือยืดได้เลย
- เปิดไฟล์ `index.html` ตรง ๆ ไม่ได้ ต้องเสิร์ฟผ่าน http (เช่น `python -m http.server`)
